// Fx.swift -- one small file.

import Foundation

/// Returns the sum of a and b.
func add(_ a: Int, _ b: Int) -> Int {
    return a + b  // a trailing comment
}

/// A holder for two numbers.
struct Pair {
    let a: Int
    let b: Int
}

let url = "http://example.com/not-a-comment"
