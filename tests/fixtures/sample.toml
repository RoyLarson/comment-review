# fx.toml -- one small manifest.
# TOML takes `#` and nothing else; `;` is not a comment here.

[project]
name = "fx"

# A multi-line basic string crosses lines, which is why `"""` is
# declared as a spanning quote.
description = """
one line
and a second
"""

url = "http://example.com/not-a-comment"
