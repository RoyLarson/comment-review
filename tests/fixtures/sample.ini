; fx.ini -- one small config.
; The semicolon is INI's original comment marker and TOML has no such
; form, which is why the two languages hold separate rows.

[server]
# The hash is the later convention and is equally live.
host = localhost
port = 8080  ; a trailing comment

[client]
url = http://example.com/not-a-comment
