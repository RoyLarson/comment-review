# 0.2.4 REWORK -- the boundaries are not real

**Branch `refactor/the-boundaries-are-not-real`, which branches off
`fix/the-binder-hands-a-reviewer-the-repo` and merges back toward
`feat/0.2.4-what-a-reviewer-is-handed`. The work is MOVING THE CODE so the responsibilities
it claims are the responsibilities it has.**

!! **THIS TURNED INTO A REAL PROJECT AND IS STILL SHAPED LIKE A BUNCH OF ONE-OFF SCRIPTS.**
Roy, 2026-08-24: *"This turned into a Real project and we have been treating it like just a
bunch of one-off scripts and so the responsibilities and boundaries and everything is all mixed
up."*

! **AND THE BINDER TRIM STOPS UNTIL THIS LANDS.** Roy: *"while I want to actually move forward
we have to fix this first."* The field cut is real and half-landed on the branch beneath this
one; continuing it would sort fields inside modules that are about to move.

## What is measured, and it is one defect wearing eight names

Every finding of 2026-08-24 is the same finding. Filed separately, they read as eight problems:

| what was filed | what it is |
| --- | --- |
| a row carries 19 fields | **nothing owns what a row IS.** `census.emitted_row` is the first statement of it, written that day |
| `vars(b)` is the wire format | **the dataclass's internals ARE the protocol**, so held and emitted could never diverge without breaking silently |
| `transcribes` lives in `tests/` | **the question has no owner** -- the galley reads it as *the file moved*, a census test as *the lexer stored it wrong* |
| there is no `test_lexer.py` | **there is no seam to test at**; every lexer invariant is asserted through `page_for` and a census row |
| nothing carries an answer to the page | **nothing owns the middle**, and two shipped sentences describe it contradictorily |
| `tier` on 659 rows for one counter | a field put where it was convenient |
| `symbol` holding `d0` | a fifth series' label in a field named for something else |
| eleven annotations, three writers | **the field has no owner**, so `lexer`, `annotate` and `census` all mutate it |

!! **THE IMPORT GRAPH IS GOVERNED AND THE DATA CONTRACTS ARE NOT**, which is why the code
LOOKS well structured. MEASURED 2026-08-24: `tests/test_page.py::TestTheTwoLeaves` enforces
EIGHT boundary rules -- the addresser knows nothing about prose, the lexer nothing about
places, `language` is a leaf with exactly two readers, `constants` is imported whole and
imports nothing back. **All eight are import rules. Not one says what a module EMITS, who may
read which field, or what one stage hands the next.** Every defect above landed in that gap.

!! **AND TEN OF NINETEEN MODULES HAVE A `main()`.** MEASURED 2026-08-24 -- four of them are
imported by other modules while also being CLIs: `addresser` (imported by **7**), `record`
(3), `compositor` (1), `vocabulary` (1). ! And `constants.utf8_console()` is CALLED from
**eleven** entry points because of it -- one per `main()`.

! **THAT LAST ONE IS NOT A DEFECT AND THIS PLAN SAID IT WAS.** The guard is DEFINED once, in
`constants.py`; eleven call sites is what correct factoring looks like when there are eleven
commands. Corrected 2026-08-24 by re-deriving it -- the first draft asserted the guard was
*"written into eleven files"*, which a `grep -l reconfigure` answers with **1**. ! It is kept
here because the number is real and only its reading was wrong, and because a plan that
measures its own premises is the thing this branch exists to argue for.

## What CORRECT means here, and it is not the suite

!! **Roy, 2026-08-24:** *"Correct isn't passing green tests -- correct is passing the human
comment review and having it come back looks good and correct."* And: *"it is ultimately that I
am satisfied with the way the code operates, not just that it can accomplish its goals but that
I agree that the way it gets from point A to B to ... Z is what I would write if I had the
time."*

! **THAT IS THE SAME STANDARD THIS TOOL APPLIES TO EVERYONE ELSE'S CODE**, which is why stage 8
is a reader rather than a checker, and why `docs/gates.md` exists. A box below being ticked is
necessary and is not the claim.

## The gate

!! **EVERY BOX TICKED, and every box names the `T` tasks it works.** ! A `*` box is a ruling
only Roy can make. **No progress count** -- the boxes are the state.

## The shape

```
src/comment_review/
  io/            repo (git, the filesystem, the exception tuples) · constants · exceptions
  ??             code_names · referrers' library half        <- P11, UNRULED
  reading/       lexer · language · the addresser's place-emitting half
  binder/        page · held · record's seeding half · the row shape
                 the addresser's census-query half · annotations
  desk/          run_context · vocabulary        (outbound)
                 desk · verdicts · record's checking half   (inbound)
  results/       galley · compositor · prove_unchanged
  flows/         census's flow half · the results-side flow
  commands/      every main() and argparse · the one console guard
```

!! **THE RULE UNDERNEATH IT, ruled 2026-08-24: A LIBRARY MODULE DOES ONE JOB AND HAS NO CLI; A
FLOW CALLS LIBRARIES; A COMMAND EXPOSES A FLOW.** Today all three collapse into each file.
! Roy, on `prove_unchanged`: *"falls in with the galley area and the process flow for that side
calls it. It probably has its own cli and shouldn't."*

! **THE SHIPPED TREE GETS THE SAME STRUCTURE, WHOLESALE.** Roy: *"It is definitely not
flattening them out again. I said wholesale I meant it. Whatever structure we end up with ends
up there."* ! Which forces the invocation: **a file inside a package cannot be run by path with
relative imports working**, so the commands run through an entry point rather than through
scripts doing double duty.

## The steps

- [ ] **P1 -- `src/comment_review/` holds the nineteen shipped Python files.** Relative
      imports, no path shims. Verify: no `sys.path.insert` anywhere in the package.

- [ ] **P2 -- the entry point is its own file, and no library module has a `main()`.** Roy:
      *"The entry points get an actual entry point .py file and the commands run through it not
      through the scripts that are doing double or triple duty."* Verify: `grep "def main"`
      over the package returns only `commands/`.

- [ ] **P3 -- the console guard is called where the console is ENTERED, once.** It follows
      `main()` into `commands/` rather than being called from eleven library modules. ! The
      guard's DEFINITION does not move and does not change. Verify:
      `constants.utf8_console()` has one caller, and `tests/test_shipped_cli_encoding.py`
      still fails if that call is deleted.

- [ ] **P4 -- `scripts/` carries the build command, and it copies the tree WHOLESALE.**
      Verify: the built tree is structurally identical to `src/`, sub-packages and all.

- [ ] **P5 -- the build is verified, not trusted.** Verify: a gate refuses a built file that
      differs from its source, and it FAILS when one is edited by hand.

- [ ] **P6 -- `SKILL.md` and the references invoke the entry point.** A one-for-one
      substitution, no instruction changed. Verify: no shipped command names a script path.

- [ ] **P7 -- tests import the package, and `tests/` FOLLOWS THE SAME LAYOUT.** Roy,
      2026-08-24: *"The tests folder layout follows the move layout."* So
      `tests/reading/test_lexer.py`, `tests/binder/test_page.py`, and a directory that is
      empty says its area is untested. Verify: every `tests/` subdirectory names a package
      under `src/comment_review/`, and `_paths.SCRIPTS` is gone.

- [ ] **P8 -- the gates read the source.** `check_shipped_syntax`, `test_shipped_imports`,
      `check_vocabulary`, `dead_sweep`. Verify: each names `src/` and not `plugins/`.

- [ ] **P9 -- the release runs the build and commits the result.** Verify: `CLAUDE.md`'s
      release section names the step. ! `plugins/` STAYS COMMITTED -- the marketplace install
      reads committed state, so a gitignored build output breaks installation.

- [ ] **P10 -- the four double-duty modules are split.** `addresser` (3 subjects), `record`
      (4), `census` (flow, row shape, name corpus), `vocabulary`. Verify: each new module's
      docstring announces ONE subject and `module-context` would not flag it.

- [ ] * **P11 -- RULE where `code_names` and `referrers`' library half live.** Roy,
      2026-08-24: *"I think there was the git stuff which is io, and the code_names and
      referrers we actually need to settle."* **The git stuff is RULED: `repo`'s git calls,
      the filesystem reads and the exception tuples are `io`.** What is open is the two that
      ask a question ABOUT the checkout rather than performing an operation ON it -- a
      git-grep for who NAMES a file, an AST walk for what a symbol is CALLED. ! Neither is
      an io operation and neither belongs to a page, so **io** and **binder** both stretch to
      hold them. Verify: the ruling is in `docs/decision-log.md`, and each module sits where
      it names.

## What is NOT in this branch

- **The binder field cut.** Ruled 2026-08-24 and half-landed beneath this branch; it resumes
  after the move, against modules that are not about to move.
- **The sorting of the nine library modules beyond these seven areas.** Roy left it open
  deliberately: *"it requires more thought than the other 2 because ... there is more
  consequence to it."*
- **What carries an agent's answer to the page.**
  [`nothing-makes-the-fair-copy`](../../TODO/nothing-makes-the-fair-copy.md) -- unnamed and
  unruled, and twelve tests are held because of it.
- **The error boundary.** [`exception-hierarchy`](../../TODO/exception-hierarchy.md) owns it,
  and the lexer's three diagnostics go when it lands.
