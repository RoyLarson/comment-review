# 0.2.4 -- the commands for the middle

```
Status:   open
Progress: 3 of 9 TODO tasks closed
Plan-tasks: 40 of 61 plan tasks closed
Owner:    backend
Requires-Roy: true
Raised:   2026-08-29 (Roy, after the chain was driven end to end for the first time
          with a real reviewer agent: "Lets work on the commands for the middle. I
          think this is a set of commands. They break down into the one(s) for the
          reviewers. The ones for task agent (managing editor) and the ones for the
          copy-chief, includes the automatic resolutions, the revise steps, and the
          collation to single master proof - edit-copy")
```

## Objective

**Every step of the chain is a command except the middle.** Measured 2026-08-29 by
running it end to end against two real files, with the shipped `block-context` agent
filling the sheet:

    census --json --out          a binder                        COMMAND
    mark --seed                  10 places for block-context     COMMAND
    the block-context agent      12 marks over those 10 places   AGENT
    mark --check                 12 ruled on, 0 left unruled     COMMAND
    gather / places / reconcile  settled 4, escalated 0          NO COMMAND
    docket_from                  2 pages, 4 alterations          NO COMMAND
    proof --docket               a revise, 0 refusals            COMMAND
    taken_in                     the diff                        COMMAND

`gather`, `places`, `reconcile` and `docket_from` are built and tested, and nothing
exposes them -- so that run drove them from a hand-written script. **The task agent
drives this pipeline by RUNNING COMMANDS, so it cannot reach reconciliation at all.**

! **THE REVIEWER HALF NEEDS NOTHING.** `mark --shape` and `mark --check` exist and a
reviewer neither seeds its own copy nor should be able to.

!! **AND THE COPY-CHIEF'S EDIT_COPY IS THE FINAL ONE.** Roy, 2026-08-29: the collation
produces one more `edit_copy` -- the copy chief's -- and the docket is a TRANSCRIPTION
of it rather than a computation over reconciliation's buckets. ! That is what makes
`docket_from` take ONE argument: it reaches into the `master_proof` today only to find
each page's `path` and `sha`, and an `edit_copy` already carries both on its sheets.

! **`Process: #42` STANDS: the copy-chief AGENT is 0.2.5.** Roy, 2026-08-29: *"the
copy-chief role is not in 0.2.4 it will be in 0.2.5. In 0.2.4 it will be handled by the
task agent. The commands for the copy-chief need to be in this release else there is
nothing to for the copy-chief todo."*

### How this plan is worked -- six superpowers plans, not one

!! **A SINGLE SP FOR THIS PLAN WAS WRITTEN AND IS THE WRONG SHAPE.** Roy, 2026-08-30: *"We don't
need 1 superpowers plan for this plan we probably need 4 or 5 that is why SP->P->T. Because each
scopes work appropriately to those things that can be known when they are created."* ! The first
one covered 20 steps; the plan reached 33 the same day, and **every addition came from something
MEASURED rather than anticipated** -- the unwired verification, the missing coverage, the
container types, the topology system. **A plan that grows like that goes stale by construction
when one SP tries to hold it.**

| | steps | |
| --- | --- | --- |
| **SP-1** | `P36`, `P35`, `P34`, `P13`, `P1`, `P2`, `P24`, `P3`, `P37` | **the containers and the collate flow** |
| **SP-2** | `P21`, `P25`, `P27` | the container wiring, source verification, shard coverage |
| **SP-3** | `P26`, `P29`-`P33` | topology as a configuration system |
| **SP-4** | `P4`, `P5`, `P14`, `P23`, `P28` | the docket, `recast`, the binder `pull` emits |
| **SP-5** | `P6`, `P8`, `P12`, `P18`, `P19` | the revise round -- where PROVISIONAL lives |
| **SP-6** | `P9`, `P10`, `P11`, `P22` | sequencing, the gates, the record |
| **SP-7** | `P39`-`P46`, and `P21`, `P25`, `P27` again | **the containers carry the flow** -- and it runs NEXT |

!!! **SP-2 IS NOT DONE, AND ITS THREE STEPS ARE REOPENED AS OF 2026-08-31.** Roy, refusing the
merge: *"You didn't do this correctly so it can't be closed. Bringing the branch back to the
other says it is done and it is correct. Passing dicts around when containers were specified as
the design pattern is not correct. It may pass tests but tests don't test correctness they test
functionality."*

!! **WHAT SP-2 DELIVERED IS THE BEHAVIOUR ON THE WRONG CARRIER.** Every verify text it was
ticked against is literally met -- `parse_edit_copy` is called at the boundary, `verify_report`
runs over every ruled mark, a short shard is named -- and every one of them operates on a raw
dict. The containers are wired as VALIDATORS whose output is discarded. `P21`'s third clause,
*"every field declared is one the code reads"*, was ticked on the reading that `seed` writes
each field and `parse` validates it; almost nothing READS one off a parsed object.

! **THE GAP WAS KNOWN AND FILED RATHER THAN TREATED AS UNFINISHED WORK.** It went into T25 the
same day and became `Process: #65`. Filing a defect does not close the step the defect is in.

!! **AND THE SUITE WAS GREEN THROUGHOUT, WHICH IS THE POINT.** Roy: *"tests don't test
correctness they test functionality."* 1,420 passing, every gate clean, two `/simplify` passes
and two `/code-review high` rounds -- and the design pattern was not followed. This repo's own
header says it: a green gate is not evidence of a good result.

! **THEY ARE REOPENED, NOT SUPERSEDED.** The work is still wanted and its verify texts still
describe it; what was wrong was ticking them while the flow carried dicts. SP-7 closes them
alongside `P42`, when the thing they describe is carried by a container.

!! **TWO OF THE THREE ARE NOW CLOSED FOR REAL, AGAINST `f1db0e5`.** `P25`'s three
source-verification checks run over every ruled mark of an `EditCopy`, and `P27`'s shard
coverage runs over `list[EditCopy]` -- both on the carrier the design specified, and both
reported by a RUN of the flow rather than by a call to the function.

!! **`P21` IS THE ONE THAT DID NOT, AND ITS THIRD CLAUSE IS WHY.** *"Every field declared is
one the code reads"* still fails: `MasterProof.stage` is read only by `MasterProof.serialize`,
and `P42` took the last production caller of `serialize` and `deserialize` both when it removed
the proof boundary -- a master_proof never reaches disk. ! **THAT IS A RULING, NOT A CLEANUP**
(`containers-and-verification-are-unwired` T37, `needs-ruling`): whether a master_proof is ever
written out decides whether the pair and the field are wanted. ! Clause TWO is met -- the
chief's copy is proved to parse as an ordinary `EditCopy` by a test that reaches `_chief_copy`
through a real `collate` run.

!! **SP-7 WAS ADDED 2026-08-31 AND RUNS BEFORE THE REST.** Roy, on being shown what the flows
actually do: *"the middle and the write are Broken. The Read is a little Broken with the
missing container at the very end."* ! **AND IT IS FIRST BECAUSE EVERYTHING ELSE IS WRITTEN
AGAINST DICTS.** Every middle function takes `binder: dict` because there is no `Binder` to
take; a step that lands before `P46` is a step that has to be written twice.

!! **THE SHAPE EVERY FLOW MUST HAVE**, ruled `Process: #65` and extended here to the ends:

    json.loads  ->  DESERIALIZE  ->  the work, on containers  ->  SERIALIZE  ->  json.dumps

! **NO RAW DICTIONARY PAST EITHER END.** Roy, 2026-08-31. ! The read and write flows are the
same shape with TEXT where the middle has json.

!!! **THE LOAD STEP BELONGS TO THE FLOW, NOT TO THE MODULE'S CODE.** Roy, 2026-08-31, stating
it again and more precisely: *"all flows start with a load step - not the modules code. If
json.loads is appropriate it does that. The text is flowed to the next step if it is text or
the loaded dict is. Then the pieces start their processing and only module components are
passed to each step until the output which either the component serializes itself and then the
flow dumps it if the change is a code file it outputs the file through machine/ code."*

!! **SO A READER THAT DECODES ITS OWN TEXT IS ITSELF THE DEFECT.** `binder.read(text)` and
`docket.read(text)` each take TEXT and call `machine.json_object.object_of`, which holds the
`json.loads`. Under this shape the flow loads and `deserialize` takes **what the load
produced** -- an already-decoded dict, never text and never a path. ! `object_of` does not go:
it is the LOAD, and it moves to the end that owns one.

!!! **`P38` IS SUPERSEDED BY `P46`, NOT REWORDED, AND THAT IS THE POINT OF THE MARK.** Its
verify read *"binder.read returns a Binder"*, which keeps the decode inside the module -- so
the step as written could be delivered in full and still leave the flow wrong. ! **THE FIRST
ATTEMPT AT THIS WAS A HAND EDIT OF `P38`'s LABEL**, which is the one thing that leaves no
record: a reworded box reads afterward as though it had always said the new thing, and the
reason it changed is nowhere. Roy, 2026-08-31: *"No reword for a reason - superseded is the
term and add it the fix."* ! **THE TOOL HAS NO REWORD VERB DELIBERATELY**, and reaching past
it with `Edit` is the same bypass as widening a verb list to make a label pass.

!! **AND THE CONTRACT IS EVERY CONTAINER'S, WHICH IS WHY `P44` EXISTS.** Roy, the same day:
*"Every container needs to be wired to do this."* Four containers exist and no two are spelled
alike -- `Sheet`, `EditCopy` and `MasterProof` are read by free-standing `parse_*` functions,
`Mark` by a module-level `parse` and written by `as_entry`. **One pair, on every container:**
`deserialize` as a classmethod over an already-loaded dict, `serialize` as an instance method
returning one. ! A `seed` is untouched by this and still returns the wire dict -- `Process:
#66`.

! **THE CODE-FILE HALF IS THE SAME SENTENCE'S LAST CLAUSE, AND IS `P45`.** *"if the change is a
code file it outputs the file through `machine/` code"* -- so `results/compositor.py`'s own
`into.write_text(...)` is the write flow's version of a module owning its own I/O.

!! **VERIFIED 2026-08-31, AND ONLY ONE OF THE THREE HAS IT.** The READ flow does:
`read_text` -> `Source` -> `Page`/`Paragraph` -> `bind()` -> `json.dumps`, with `bind` as the
serialize. ! **THE WRITE FLOW DOES NOT**: `docket.read(text)` decodes, validates and returns a
DICT, and `revise.pull` and `proof_setter.run` process dicts the whole way -- while `Schedule`,
the docket's own container, sits at `docket/docket.py:80` with **no caller outside its own
module** and a docstring recording that `pull._set_by` reads *"straight off the raw docket
dict"*. ! **AND THE MIDDLE DOES NOT**: 29 signatures between the load and the save take or
return a raw dict.

!! **THE READ FLOW'S OWN GAP IS THE BOUNDARY, NOT THE MIDDLE.** `binder.read` checks exactly
two things -- that the JSON decodes, and that a `pages` key exists. Nothing checks `read_from`,
a page's `path`, or a row's `cue`, so a PARTIAL binder is admitted and every consumer
re-derives it with `.get()` defaults. Roy: a container there *"would also allow the boundary to
ensure it is loaded correctly not partially."*

! **TWO MEASURED COSTS FALL OUT WITHOUT A STEP OF THEIR OWN.** `desk.mark.parse` runs 4x per
ruled mark and `known_addresses`/`base_texts` rebuild N+1 times per `collate`, both because
every function re-derives from a dict. Carrying the parsed container removes the cause; neither
is a box, because neither is work once `P42` lands.

!! **AND SP-4 IS NOT REWRITTEN NOW.** `P4`, `P5`, `P14` and `P23` all touch what `P40` and
`P41` change. Roy, 2026-08-31: *"This first we will supersede those tasks when we get to
them."* ! **SUPERSEDING THEM EARLY WOULD BE GUESSING** at what SP-7 leaves of them; superseding
them on arrival is a reading.

! **EVERY OPEN STEP IS ASSIGNED**, checked both directions against the table above -- every open
step appears in exactly one row, and every step a row names is open or closed knowingly.
!! **THIS SENTENCE CARRIED A COUNT UNTIL 2026-08-31 AND THE COUNT WAS WRONG TWICE.** It read
*"the 23 open steps are the 23 the rows name"*, went stale when `P21`, `P25` and `P27` closed,
and was then re-stated as 29 when the true figure was 26. **That is this file's own rule
arriving on this file**: `Plan-tasks:` is recomputed from the boxes on every tick, and a second
copy in prose is a number nobody recomputes. The claim is worth keeping; the arithmetic is not. ! **THE FIVE IN NO ROW ARE `P7`, `P15`, `P16`, `P17` AND `P20`** --
two rulings and three supersessions, which are records rather than work and so have no SP to land
in. ! This sentence read *"and no closed one is"* until 2026-08-31, written while SP-1's ten steps
were all open; nine of them are closed and still named in its row, because a row says which SP
DELIVERED a step and not which are outstanding. ! **THE COUNT IS NOT RESTATED HERE**,
deliberately: `Plan-tasks:` in the header carries it
and is recomputed from the boxes on every tick. A second copy in prose is a number nobody
recomputes, and this sentence held a stale one for eight steps before anyone read it.

!! **TWO STEPS MOVED WHEN SP-1 WAS SPECIFIED, 2026-08-30, and the table above records where they
landed rather than where they started.** ! **`P24` CAME INTO SP-1 FROM SP-2**, because
`decision-log.md Process: #54` says the move lands WITH the flow -- *"`mark --check` reaches these
through `flows/collate.py`"* -- and SP-1 is where that flow is built. Leaving it in SP-2 would have
had SP-1 ship `P37`'s clause about the shape verbs one plan before `P24` made it false.
! **`P22` WENT OUT TO SP-6**, because its verify -- *"no command reports only a count"* -- is a rule
for EVERY middle command and SP-1 builds one. **The consequence is stated rather than hidden:**
`collate` ships from SP-1 with the three buckets `P3` requires and WITHOUT `Process: #51`'s
*"and here is the command that continues it"* line. SP-6 adds it, and the report is shaped so that
is an addition rather than a rewrite.

!! **`P35`, `P36` AND `P37` WERE DECIDED ON 2026-08-30 AND WERE NOT WORK UNTIL 2026-08-30.** Two of
the rulings reached `decision-log.md` -- `Process: #53` and `#54` -- and **none of the three
became a step**, so the plan carried the conclusion without the task. Roy, on finding it: *"Is lost
to the conversation even though it was 'Decided'."*

! **`P37` REACHED NOTHING AT ALL**, which is the worse half: the log has no entry either. Roy,
2026-08-30: *"The command name and the flow name and the flows need to be updated to have better
names because a flow named mark reads like it is doing something that it is probably not doing"*,
and *"the broadcasting part seems like distribute, the bringin back together seems like collate."*
! **IT IS LAST IN SP-1 BECAUSE THE PAIR IS THE VERIFY** -- `distribute` and `collate` read as two
halves of one round only once `P1` has built the second half.

!! **AND THE SWEEP THAT FOUND ALL THREE READ ROY'S OWN WORDS, NOT THE SESSION'S SUMMARY OF THEM.**
86 messages across today's transcripts, checked one at a time against the tree. Four other rulings
from the same morning WERE carried -- `recast` into `P14` quoting him verbatim, the explicit
send-back step into the log at `#51`'s neighbour, the `Schedule` pattern into `P21`, and the
empty-glob refusal into `staged-chain-untested`. **The failure is not that nothing was recorded; it
is that recording was per-ruling and nothing checked the set.**

! **A RULING IS NOT A SCHEDULE, AND THIS IS THE MEASURED COST.** `#54` says *"did this parse back
correctly"* is the mark's own question, which is exactly `Mark.seed`'s round trip -- and `P34` was
written to say `seed` writes the field *"as it does today"*, pinning the write half in
`flows/marks.py` where a rename cannot reach it. **The log said one thing and the plan said the
opposite**, because only one of them was updated. `P34`'s clause now names `P35` instead.

!! **AND TWO MORE MOVED WHEN SP-2 WAS SPECIFIED, 2026-08-31.** ! **`P21` CAME INTO SP-2 FROM
SP-1**, because its verify -- *"every field declared is one the code reads"* -- cannot be met while
nothing imports the module, and the importer it waits on is `P25`'s. MEASURED 2026-08-31:
`grep -rn "containers" src/ --include=*.py` returns five lines and **every one is prose inside a
docstring**; `verify_report`, `address_problems`, `claim_verbatim_problems` and `source_problems`
are reached only from `tests/test_collator.py`. **The consequence is stated rather than hidden:
SP-1 closes at nine of the ten steps it was specified with**, and `P21` is the tenth.

! **`P28` WENT OUT TO SP-4**, ruled by Roy 2026-08-31 over the alternative of rewording it to check
the copy chief's `edit_copy` inside `collate`. Its verify names *"every address reaching the
docket"*, and **nothing in production builds a docket**: `desk.collator.docket_from` has no caller
outside `tests/`, and `flows.collate.collate` ends at `_chief_copy`. The command that builds one is
`P4`/`P5`. ! **So the address space goes unchecked through SP-2 and SP-3**, which is the price of
not writing a check against a path no run takes.

!! **AND A THIRD MOVED, THE SAME DAY AND FOR A REASON THAT WAS NOT VISIBLE UNTIL IT WAS ASKED FOR.**
! **`P26` WENT TO SP-3.** Roy, 2026-08-31, on the draft that had `collate` alone take the `Stage`:
*"I feel that both collate and distribute have to take a stage argument because how does distribute
know to redistribute a stage output binder from the original binder?"*

!! **HE IS RIGHT, AND `Stage.reads` IS THE FIELD THAT SAYS SO.** MEASURED 2026-08-31:
`grep -rn "\.reads\b" src/comment_review/ --include=*.py` returns ONE line, `desk/topology.py:140`,
and it is the WRITE. `topology.py:84-95` refuses a `"revise:<name>"` naming a stage that pulls no
revise, BY NAME -- **so the value is validated on the way in and consumed by nobody after.**
`flows.fan_out.fan` has no production caller either, so `stage.dispatches` is unread by the same
measurement. ! **THE COST IS A SILENT WRONG ANSWER**: `distribute --seed --binder <path>` takes the
tree from whoever typed the path, so a stage 2 pointed at the original binder instead of revise-1
hands every role stale text and every address still resolves. Filed as
[`staged-chain-untested`](../../TODO/staged-chain-untested.md) T4.

! **SO THE STAGE ARGUMENT IS ONE ACT ON BOTH HALVES, AND IT BELONGS WHERE THE TOPOLOGY IS BUILT.**
`P32` has the agent VERIFY before it distributes; a consumer wired in SP-2 would read a
configuration nothing has verified. **SP-2 keeps only what the binder and the containers answer for
themselves** -- which is why `P27` stays: `collate` already takes the WHOLE binder, so shard
coverage is `fan`'s own `UncoveredPage` check run over the RETURN, with no new input at all.
! **THE PRICE IS STATED: no stage coverage until SP-3, and `Stage.reads` unread until then.**

! **`P36` LEADS SP-1 rather than joining SP-2's collator work**, because the false claim it removes
is the one that already misled a session into ruling the compose out of reconciliation. Building
`P13` first would shape the compose around a constraint `#53` says does not exist.

! **`P13` -- THE COMPOSE -- WAS PULLED FORWARD INTO SP-1**, Roy 2026-08-30: *"Lets pull in P13 -
so it is a clean forward run to a decision point."* `P2`'s resolutions include a re-read whose
sides compose cleanly, so a resolve step without the compose is not the resolve half of
collate/resolve.

! **AND `P34` IS WHAT LETS IT COMPOSE AT ALL.** The base paragraph does not survive `places()`
today -- `Placed` is `(mark, role)` and `raw_text` is threaded as a loose argument to
`claim_verbatim_problems`. Putting it on the `Mark` as a third seeded field gives `Placed` the
base for free, with no nesting.

! **SP-3 LANDS ANYWHERE BEFORE SP-6.** `P32` -- verify before distribute -- changes the workflow
SP-6's sequencing assumes, so it must precede it; nothing else depends on the order.

! **SP-5 IS LAST OF THE SUBSTANTIVE ONES, because it is the least settled.** The round-2
`edit_copy` shape is PROVISIONAL by Roy's own ruling, 2026-08-30: *"an edit-copy may not be the
correct thing to pass back but we should test this out by implementing this with PROVISIONAL
until the shape can be better determined."*

!! **SP-1 IS THE CONTAINERS AND THE FLOW, NOT THE TOPOLOGY, AND THE REASON IS COST RATHER THAN
EASE.** Topology is the more isolated piece and was the instinct. But its system GUARDS
`distribute` rather than altering it, while **the middle has no command at all** -- which is what
blocks every other stage. ! The argument for waiting was that the container work looked unsettled,
having been rewritten twice in one day; it moved because it was being SETTLED -- the `Schedule`
pattern, dicts on the wire, the gate inside the task, containers in `desk/` -- and those are now
ruled.

### Not in scope

! **`prove_unchanged` is not a middle command.** It is stage 7b's, run by
`references/write.md` against the PRE-EDIT REF after WRITE reaches the real tree.
Inside the revise chain `proof_setter` already proves each draft, so a second prove
there would be redundant. Roy, 2026-08-29: *"It is the responsibility of the
proof-setter pass."*

## TODO tasks this plan closes

### A - [no-command-for-the-middle](../../TODO/no-command-for-the-middle.md)

- [ ] T1 | Implement the step that turns the copy chief's edit_copy into the
      docket `proof --docket` reads. Verify: with the filled edit_copies of a
      stage on disk, the chain census -> seed -> collate -> <this> -> proof runs
      with no Python written by hand. !! SUPERSEDED IN PART 2026-08-30, AND THIS
      IS THE REMAINDER. It read "A command turns one stage's checked edit_copies
      into a docket ... ONE command writes the docket `proof --docket` reads",
      and it was TICKED against that wording when `collate` landed. A review
      read the verify against what shipped and it is false: `collate` writes the
      copy chief's `edit_copy`, which is an ordinary edit_copy -- `{"role",
      "read_from", "sheets"}` -- and `proof --docket` reads `{"pages": [{"path",
      "sha", "alterations": [{"cue", "text"}]}]}`. `commands/proof.py` would
      refuse the one given the other. ! WHAT DID LAND IS THE FOLD, and it is
      real: one stage's checked copies gathered, reconciled, automatically
      resolved where they can be, and folded into one copy with one mark per
      resolved place. The three tasks below it are ticked on their own terms and
      are unaffected. ! WHAT IS OWED HERE IS THE BRIDGE -- transcribing that
      copy into a docket, which is a known piece of work with its own scope
      elsewhere. This box stays open until the chain runs through to `proof`.
- [x] T2 | It names the ESCALATED and RE-READ places, not just what settled.
      Verify: both appear on stdout -- `docket_from` packages only the settled
      ones, so a run that settles 4 of 10 says which of the other 6 escalated
      and which owe a re-read. ! Narrowed 2026-08-30; the unruled remainder is a
      task below.
- [x] T3 | Its exit code reaches three different values for the three outcomes.
      Verify: a stage that settles everything, one that escalates and one that
      owes a re-read do not share a code. ! Narrowed 2026-08-30 from
      "distinguishable by exit code alone"; see the note.
- [ ] T4 | `tests/gates/test_skill_commands.py` sees it. Verify: the command
      appears in `COMMANDS`, `--help` names it, and the gate that checks
      SKILL.md's commands resolve covers this one.
- [-] T5 | A command SEQUENCES the stages a topology names. Verify: one
      invocation runs stage 1, pulls revise-1, runs stage 2 against that revise,
      and stops -- the topology already expresses the order and `fan_out`
      already partitions, but nothing drives them.
- [?] T10 | Implement the command a reviewer runs to write one mark into its
      edit_copy, so a role does not hand-write JSON

### B - [a-revise-answer-has-no-artifact](../../TODO/a-revise-answer-has-no-artifact.md)

- [ ] T4 | MOVED to 0.2.4-the-mark-and-the-collator | T4 -- Route a `query` by
      shape. Verify: `human-review-necessary` never returns to a role, and
      `unable-to-determine` carries the question into the next ask.
- [ ] T5 | MOVED to 0.2.4-the-mark-and-the-collator | T5 -- Count queries first
      raised at revise. Verify: the run reports the number, and it is zero on a
      set of marks where every query was raised in round one.
- [ ] T7 | MOVED to 0.2.4-the-mark-and-the-collator | T7 -- A reversal is a row,
      paired with whoever LAST set the statement. Verify: stage 3 reversing a
      paragraph stage 2 set pairs with stage 2 and not with stage 1, read from
      the revise's per-place provenance.

## Plan Tasks

- [x] P1 | Implement `flows/collate.py` -- ONE flow running gather, reconcile and the
      automatic resolutions, writing the copy-chief's `edit_copy`. Roy: *"I think
      collate resolve is one flow."*
- [x] P2 | Implement the automatic resolutions inside that flow: a place where one mark
      owes a change, and a place where every owing mark proposes byte-identical text.
      What it cannot resolve it carries forward NAMED, resolving nothing on its own.
- [x] P3 | Implement the `collate` command exposing P1, reporting the three buckets on
      stdout and separating them by exit code. Closes A-T2, A-T3.
      !! A-T1 DID NOT CLOSE. Ticked here would have repeated the same false claim its
      own wording made -- "ONE command writes the docket `proof --docket` reads" --
      which review found false of what shipped: `collate` writes the copy chief's
      `edit_copy`, and `proof --docket` reads a docket; `commands/proof.py` refuses one
      given the other. Unticked and reworded to the remainder of
      `no-command-for-the-middle.md` in `7bc5887`; tracked there, not repeated here.
- [-] P4 | SUPERSEDED by P53-P56 -- docket_from does not change signature, it leaves desk/ entirely; the transcribe is flows/revise.py's | 35f996e | Update
      `desk.collator.docket_from` to transcribe the copy-chief's `edit_copy` --
      one argument, not `(reconciled, proof)` -- since an `edit_copy` carries
      each page's `path` and `sha` on its sheets.
- [-] P5 | SUPERSEDED by P57-P59 -- there is no docket command; proof takes --copy and transcribes on its first step, with --from-docket and --to-docket as the boundaries | 35f996e | Implement
      the `docket` command exposing P4.
- [-] P6 | SUPERSEDED -- the revise step is owned by 0.2.4-the-mark-and-the-collator, which the branch is named for. Roy 2026-09-01: supersede them in both locations and recreate them here as owned here. Its TODO tasks moved with it. | 10a87e3 | Implement
      the diff-mark as its OWN artifact carrying one of four -- `hold`,
      `withdraw`, `correct`, `patch` -- per `decision-log.md Process: #22`. ! It
      is NOT a `Mark` and NOT an eighth instruction: *"a diff-mark is a
      DIFFERENT ARTIFACT answering a different question -- does your finding
      still stand rather than what is wrong with this page -- so it carries its
      own closed set. The seven stay seven."* ! **An unanswered place is NOT an
      inferred `withdraw`** -- the null answer must be written by a hand, which
      is `#22`'s whole point.
- [-] P7 | SUPERSEDED by P22 -- Process 51. A round tally was carried state guarding an accident that cannot happen: nothing advances a round but a command someone invokes. What was missing was not enforcement but NOTIFICATION | 4376bea | Implement
      the round tally on the copy-chief's `edit_copy`, per place and BY KIND. Roy,
      2026-08-29: the chief's `edit_copy` is the one artifact that persists across
      rounds, so a stranger reads the count off the file rather than re-deriving
      it from how many revises exist. Closes the state half of `T6.11`.
- [-] P12 | SUPERSEDED -- the revise step is owned by 0.2.4-the-mark-and-the-collator, which the branch is named for. Roy 2026-09-01: supersede them in both locations and recreate them here as owned here. Its TODO tasks moved with it. | 10a87e3 | Update
      the diff-mark's payload to carry the `diff3` render, so each role sees the
      other's edit rather than only its own. Roy, 2026-08-29: *"it was supposed
      to carry a diff3 as part of the payload so each reviewer can see the edit
      of the other."* ! The RENDER already exists --
      `a-conflict-has-no-rendering` T1, T2 and T4 are done; what is missing is
      putting it on the row a role reads.
- [x] P13 | Implement the COMPOSE: apply two edits to the base when the spans they
      touch are disjoint, and REFUSE when they overlap. Verify: two edits on different
      sentences of one paragraph merge to a paragraph carrying both, and two edits on
      the same sentence refuse by name rather than picking a side.
      ! **THIS IS WHAT ANSWERS "HOW DO BOTH GET TAKEN IN", AND IT NEEDS NO FIFTH
      ANSWER.** Roy asked *"how do the agents get to say both are true and should be
      taken_in"*; `hold` already says *my mark stands*, so two holds on disjoint spans
      are saying it, and the composition is then ARITHMETIC.
      ! **`diff3` DOES NOT DO THIS.** It RENDERS -- *"every span at least one side
      edited wrapped in a conflict span"*, even where only one side touched it. A
      compose ACTS on the disjointness that render only shows.
      ! **AND IT IS WHY THE CHIEF TRANSCRIBES NOTHING IN THE ORDINARY CASE.** A derived
      composition can be re-derived and checked, and it was set by BOTH roles -- which
      `set_by` can carry. A hand transcription attributes to the chief, who wrote none
      of it.
- [ ] P14 | Implement `recast` -- the copy chief's OWN prose, replacing every side. Its
      third act, alongside `taken_in` and `stet`; `decision-log.md Vocabulary: #29`.
      Roy, 2026-08-29: *"the chief-composes-by-hand needs to be available no matter what
      ... `copy-chief-edit --sheet x --mark-address b3 --replace "Resolved New Prose"`
      which is different than stet or taken_in."* Verify: the chief can set a place's
      text to prose neither role proposed, the docket records the CHIEF as who set it,
      and the act is reachable when a compose refused.
      ! **IT EXISTS FOR THE CASE THAT SHOULD NOT ARISE**: no clean composition, or
      genuine disagreement where neither side is right and neither can be made right
      from its own statement. Roy: *"This should never happen but..."* ! A path with no
      answer is where a run stops dead, so the unlikely branch is the one that must
      exist.
- [x] P15 | RULED: the chief's third act is `recast` | a061ee3 | Roy, 2026-08-30,
      *"recast works"*. Recorded as `decision-log.md Vocabulary: #29`.
- [-] P8 | SUPERSEDED -- the revise step is owned by 0.2.4-the-mark-and-the-collator, which the branch is named for. Roy 2026-09-01: supersede them in both locations and recreate them here as owned here. Its TODO tasks moved with it. | 10a87e3 | Implement
      the revise round -- resend to the parties, collate, resolve -- over
      diff-marks rather than plain marks, writing back into the copy-chief's
      `edit_copy`. Closes B-T4, B-T5, B-T7.
- [-] P9 | SUPERSEDED by Process #73 -- the task agent sequences from SKILL.md; #74 keeps the topology's DISPATCHES as a command's input, not its order | 35f996e | Implement
      the sequencing command that runs the stages a topology names. Closes A-T5.
- [ ] P10 | Update `tests/gates/test_skill_commands.py` and `SKILL.md` so every new
      command is named where the gate can see it. Closes A-T4.
- [ ] P11 | Update `docs/decision-log.md` with the rulings this plan produced, and
      `docs/vocabulary.md` where a term moved.
- [-] P16 | SUPERSEDED by P21 -- it named docs/the-mark.md, which owns the MARK's fields and not the containers that hold marks. Vocabulary #30 puts the type in desk/ instead | c2c8af2 | Update
      docs/the-mark.md to DEFINE the edit_copy -- the ordinary one and the copy
      chief's. Verify: the file states both shapes, and a test reads the shape out
      of that file rather than restating it, so the code cannot drift from the
      source Process: 37 already names
- [-] P17 | SUPERSEDED -- it asked for three states on an entry. Those are the INTERMEDIATE and already have a type, desk.collator.Reconciled. The chief's copy is the fold's RESULT and carries one mark per place, per Vocabulary #30 | c2c8af2 | Update
      docs/the-mark.md to state what an entry in the copy chief's edit_copy
      carries in EACH of its three states -- settled, escalated, and sent back for
      a re-read. Verify: the three are named with their fields, and a reader can
      tell from an entry alone which state it is in without consulting the run
      that produced it
- [-] P18 | SUPERSEDED -- the revise step is owned by 0.2.4-the-mark-and-the-collator, which the branch is named for. Roy 2026-09-01: supersede them in both locations and recreate them here as owned here. Its TODO tasks moved with it. | 10a87e3 | Implement
      the tool that renders a diff3 onto a revise row, so a role is handed the
      base and every side rather than only its own edit. Verify: a row for a
      place two roles edited carries the render, a role that edited nothing at
      that place still receives it, and the render is the one
      results/differences.diff3 already produces rather than a second one
- [-] P19 | SUPERSEDED -- the revise step is owned by 0.2.4-the-mark-and-the-collator, which the branch is named for. Roy 2026-09-01: supersede them in both locations and recreate them here as owned here. Its TODO tasks moved with it. | 10a87e3 | Implement
      the gate that a diff3 payload is READ and never PARSED. Verify: no module
      under src/comment_review imports or defines a parser for the render, and a
      test fails if one appears. This is the only thing keeping the prototype's
      delimiter failure away, since a conflict marker is a string a comment may
      legitimately contain
- [x] P20 | RULED: a revise is ALWAYS pulled, so reads revise-N always names a real tree. An empty docket yields a copy with zero overlays -- the revise is the tree the next stage reads and the binder it is censused from, not only the edits. Process 52 | 65e3884 | What
      does reads revise-N mean when stage N settled nothing? A stage that
      escalates everything produces no alterations, so its revise is either absent
      or byte-identical to what it read. Nothing in the topology, the fan-out or
      revise.pull decides which, and the next stage's read names it either way
- [ ] P21 | Update desk/containers.py so its second and third verify clauses
      hold. The dataclasses and the boundary parse LANDED and clause one is met
      -- the type matches what seed() builds over a real tree. Verify: the
      chief's copy parses as an ordinary EditCopy with no second shape, proved
      by a test that CALLS _chief_copy; and every field declared is one the code
      reads. ! UNTICKED 2026-08-30, superseded in part. Measured by the
      containers review: the chief's-copy test relabels a seed copy instead of
      calling _chief_copy, so clause two was never exercised; and containers has
      no production importer, so clause three cannot be met until the wiring
      lands -- TODO/containers-and-verification-are-unwired.md. ! Ticking it
      asserted a claim two thirds of which was untested, which is the error the
      tick step exists to prevent: read the box's own verify text before ticking
      it
        > 2026-08-31 P42 met clause two and not clause three. The chief's copy is
        > 2026-08-31 now proved to parse as an ordinary EditCopy by a test that reaches
        > 2026-08-31 _chief_copy through a real collate run. Clause three still fails:
        > 2026-08-31 MasterProof.stage is read only by MasterProof.serialize, and that
        > 2026-08-31 pair has no production caller left -- a master_proof never reaches
        > 2026-08-31 disk. Filed on containers-and-verification-are-unwired.
- [ ] P22 | Implement the report every middle command ends with, per Process 51:
      name what remains unresolved AND the command that continues it. Verify: a
      stage settling 4 of 10 names the other 6 and prints the invocation that
      sends them back, a stage settling everything says so and names nothing, and
      no command reports only a count the way mark --check does today
- [ ] P23 | Update flows/revise.pull to EMIT the stage's binder alongside the
      revise, per Process 52. Verify: Pulled carries a binder censused from the
      assembled root with read_from naming that revise, a docket with zero
      alterations still produces both, and it is a SECOND bind rather than the
      gate's -- assert_addresses_held binds with absent True, which carries every
      empty place and is not what a role may be handed
- [x] P24 | Update flows/marks.py and desk/collator.py so unruled, problems_in and
      tally sit with the collator, per Process 54 -- a mark answers for itself and
      everything about the SET is the collator's. Verify: all three reach
      commands/mark.py through flows/collate.py rather than by importing desk
      directly, since Process 12 has a command expose a flow
- [x] P25 | collate runs address_problems, claim_verbatim_problems and source_problems over every ruled mark through verify_report, which now takes the EditCopy. A cite naming a file that does not exist and a claim quoting a sentence absent from its paragraph are each REPORTED by a run of the flow, not only by a call to the function -- tests/test_collate.py::TestSourceVerificationRunsInProduction, three cases. | f1db0e5 | Implement
      the wiring for source-verification, which is built and called by nothing
      in production. Verify: collate runs address_problems,
      claim_verbatim_problems and source_problems over every ruled mark, and a
      mark quoting a sentence absent from its paragraph or citing a file that
      does not exist is REPORTED -- neither is refusable by parse, which imports
      no binder, no page and no filesystem
- [ ] P26 | Implement STAGE coverage. Verify: collate refuses or reports a stage
      where a dispatch the topology named returned no edit_copy. Today gather
      takes whatever list it is handed and counts nothing
- [x] P27 | _coverage_problems takes list[EditCopy] and runs inside collate over the union of each role's copies, so a partitioned role that answered for three of four files in its shard is named, and so is a copy that kept one of its four seeded slots. Reported in its own list with its own exit code, never in problems -- Process 63. | f1db0e5 | Implement
      SHARD coverage. Verify: a partitioned role that answered for three of the
      four files in its shard is named. fan_out refuses overlap and uncovered
      pages at DISPATCH; nothing checks the RETURN
- [ ] P28 | Implement ADDRESS integrity over the docket. Verify: every address
      reaching the docket was one the binder carried, using known_addresses --
      which exists and is used only by flows/revise.py, for the different question
      of whether the address space moved
- [ ] P29 | Implement the topology VERIFY, which rules on whether a topology fits
      THIS checkout. Verify: it errors on a glob that matches no page, on a
      fan-out that leaves a page uncovered, and on a reads naming a stage that
      pulls no revise -- and it says which of those it is. MEASURED:
      4a-then-4c.toml refuses a scratch tree today, but from fan, blaming the tree
      for the topology's assumption and only once a run is underway
- [ ] P30 | Implement the topology BUILD, so the agent does not hand-write TOML.
      Verify: given the directives -- which roles, in what order, which one splits
      and how many ways -- it emits a topology that topology.read accepts and
      VERIFY passes against the tree it names
- [ ] P31 | Implement the topology command exposing both, per Process 12 -- a
      command exposes a flow. Verify: it appears in COMMANDS, --help names both
      modes, and tests/gates/test_skill_commands.py covers it
- [ ] P32 | Update SKILL.md so the task agent VERIFIES before it distributes.
      Verify: stage 1 names the build and the verify, and a run against an
      unverified topology is not a path the instructions offer. A bad
      configuration must cost nothing, which it only does when it is caught before
      the first page is read
- [ ] P33 | Make the fan-out fixture runnable on any tree, or state why it is not.
      Verify: either 4a-then-4c.toml drives a scratch tree of four files, or the
      topology system refuses it at VERIFY with a message naming the globs rather
      than the pages
- [x] P34 | Update Mark and docs/the-mark.md so the mark carries raw_text as a
      THIRD seeded field, beside address and anchor. Verify: the field table and
      the dataclass move together or tests/gates/test_mark_shape.py fails, the
      field reaches every slot through P35's Mark.seed rather than through a
      literal, and Placed carries the base text for free because the mark does --
      no Placed(mark, role, raw_text) and no Mark holding a Placed
- [x] P35 | Implement Mark.seed so the row a role is handed is built from Mark's
      OWN field names rather than from four literals. Verify: renaming a Mark
      field breaks at construction instead of leaving flows/marks.py writing the
      old key, the literals at flows/marks.py:82-89 come from one place, and
      raw_text rides the same round trip -- Process 54 makes did this parse back
      correctly the mark's own question, and today only the READ half lives with
      the Mark while the WRITE half is a dict literal in another layer
- [x] P36 | Update desk/collator.py so every claim in its prose is one the code
      can be checked against, per Process 53. Verify: the header no longer
      states never renders the composed text or never reads a file as constraints
      citing Vocabulary 11 -- which rules the NAME and nothing else -- and each
      function still present is one the design as laid out needs rather than one
      the retired verdicts.py left behind
- [x] P37 | Update the names on the broadcast half so a flow is named for what it
      does -- flows/marks.py becomes the DISTRIBUTE flow and the mark command
      follows it. Roy, 2026-08-30: the broadcasting part seems like distribute,
      the bringing back together seems like collate, and a flow named mark reads
      like it is doing something that it is probably not doing. Verify: no module
      under flows/ is named for the artifact it carries rather than the act it
      performs, seed reaches commands through the renamed flow -- narrowed by P24,
      landed before this task: the three check verbs reach commands/collate.py
      through flows/collate.py, not through this rename -- and the pair distribute
      and collate reads as the two halves of one round
- [-] P38 | Its verify keeps the decode inside the module -- binder.read returns a Binder -- which Process 67 rules is itself the defect. Superseded by P46, which moves the load to the flow. | 288592f | Implement
      the Binder container -- the type, its deserialize and its serialize -- so
      the read boundary refuses a partial binder by name rather than admitting
      it. Verify: binder.read returns a Binder or names every broken rule; a
      binder with no read_from, a page with no path, and a row with no cue are
      each refused by name, where today only an absent pages key is caught
        > 2026-08-31 The load moved to the flow -- Roy 2026-08-31; see SP-7 prose
- [x] P39 | bind returns a Binder and census writes json.dumps of its serialize; rows_of is gone and the round trip is asserted on the containers rather than on two wire dicts. | 1d9314d | Update
      the read flow so `bind` returns a `Binder` and `census` writes
      `json.dumps` of its serialize. Verify: the only raw dict the read flow
      produces is the one `dumps` receives, and a census over a real tree still
      round-trips through `binder.read` unchanged
- [x] P40 | Alteration, Schedule and Docket each answer deserialize over an already-loaded dict and serialize back; docket.read and schedules_of are gone; desk.collator.docket_from builds them, so Schedule has callers outside docket/. A page with no path or no sha is refused by name. | 3fa2207 | Implement
      the Docket container, promoting `Schedule` from a per-page tuple nothing
      calls into the whole artifact with a deserialize and a serialize. Verify:
      `grep -rn Schedule src/` returns a caller outside `docket/`, and a docket
      page with no `path` or no `sha` is refused by name
- [x] P41 | revise.pull and proof_setter.run take a Docket, neither signature names dict, and _set_by walks the container rather than reading the raw docket its own docstring recorded it reading. Every draft is still proved unchanged. | 3fa2207 | Update
      the write flow so `flows.revise.pull` and `flows.proof_setter.run` take a
      `Docket` rather than a raw dict, and `_set_by` stops reading the raw
      docket its own docstring records it reading. Verify: neither signature
      names `dict`, and every draft is still proved unchanged
- [x] P42 | Every function in desk/collator.py and flows/collate.py between the load and the save takes a container: EditCopy for the per-copy checks, MasterProof for the proof walkers. gather returns a MasterProof and Collated.chief is an EditCopy the command serializes. Five spellings of one sheet walk, problems_in's three header checks, _real_pages' sha fold, UnnamedRole, gather's KeyError and the proof-boundary parse all went with the types. The Reconciled entry is the one raw dict left and is filed as T36. | f1db0e5 | Update
      the middle so `seed`, `fan`, `gather` and `collate` carry containers
      between the load and the save. Verify: no function BETWEEN those two ends
      takes or returns a raw dict -- 29 signatures do today -- and
      `verify_report`, `problems_in`, `drift_in`, `unruled`, `tally` and
      `_coverage_problems` each take the container rather than a `report: dict`.
      Closes T26 and T27, and closes `P21`, `P25` and `P27`, which were ticked
      2026-08-31 against this behaviour carried on dicts and reopened the same
      day. ! A `seed` STILL RETURNS THE WIRE DICT -- `Process: #66`; it is
      emitted AT a save, and T28 is superseded
- [x] P43 | json.loads appears once in src, in machine/json_object.object_of, and json.dumps once per command that writes -- carry, census, collate, distribute. Neither name is under flows/ or desk/, and no module outside commands/ and json_object.py imports json. collate._load called object_of instead of its own loads; distribute's two branch dumps became one _as_json. | 7a49776 | Update
      every middle command so `json.loads` and `json.dumps` happen once each, at
      the ends of the flow. Verify: neither name appears under `flows/` or
      `desk/`, and no command holds more than one of each
- [x] P44 | Sheet, EditCopy, MasterProof and Mark each answer deserialize as a classmethod over an already-loaded dict and serialize as an instance method; the three parse_ functions and as_entry are gone, prose included; seed still returns the wire dict per Process 66. | dee2ad1 | Update
      Sheet, EditCopy, MasterProof and Mark onto the same deserialize and
      serialize pair the Binder gets, so every container is wired alike. Verify:
      each answers deserialize as a classmethod over an already-loaded dict and
      serialize as an instance method returning one; parse_sheet,
      parse_edit_copy, parse_master_proof, desk.mark.parse and Mark.as_entry are
      gone as free-standing spellings of the same act; and seed still returns
      the wire dict, which Process 66 settles
        > 2026-08-31 Roy 2026-08-31: every container needs to be wired to do this
- [x] P45 | machine.repo.write_raw is read_raw's pair and carries the mkdir and the untranslated write; compositor.draft calls it and results/ and flows/ hold no write_text call site. Every draft is still reread and proved unchanged -- 50 proof_setter cases pass, and the move exposed two that could no longer fail plus one that would have gone on passing against the wrong seam. | fb8c29b | Update
      the write flow so a set page reaches disk through machine/, not through
      results/compositor.py's own write_text, which is the code-file half of the
      same rule the json ends obey. Verify: no write_text is reachable from
      results/ or flows/ except through machine/, and every draft is still
      proved unchanged
- [x] P46 | Binder, BinderPage and BinderRow carry deserialize over an already-loaded dict and serialize back; binder.read is gone, object_of moved to the four commands, and no json.loads is reachable from binder.py. A binder with no read_from, a page with no path and a row with no cue are each refused by name. | 1d9314d | Implement
      the Binder container -- the type, its deserialize and its serialize -- and
      move the LOAD out of binder.py into the flow, so the read boundary refuses
      a partial binder by name rather than admitting it. Verify:
      Binder.deserialize takes an already-loaded dict, never text and never a
      path; a binder with no read_from, a page with no path, and a row with no
      cue are each refused by name, where today only an absent pages key is
      caught; and no json.loads is reachable from binder.py, which holds one
      today through machine.json_object.object_of. Supersedes P38, whose verify
      kept the decode in the module
        > 2026-08-31 Supersedes P38 -- Process 67 moved the load to the flow
- [x] P47 | BinderPage, BinderRow, page_row and the Addressed protocol are deleted; a Binder holds Page or RedactedPage and both hold Paragraphs, with kind from Series.of(cue).present and start/end from the recorded original_start/original_end. No type in src is named for its module or a wire key. | 8c916ad | Delete
      BinderPage and BinderRow and hold Paragraphs on a Page or a RedactedPage
      instead, where a RedactedPage serializes only the places holding prose.
      Verify: no type in src/ is named for the module it lives in or for a wire
      key; Binder.pages holds Page or RedactedPage; a deserialized page holds
      real Paragraphs, with kind taken from Series.of(cue).present and start/end
      from the recorded original_start/original_end; and the flow orchestrates
      the edit_copies and the master_proof from that rather than from a second
      row type
        > 2026-08-31 Roy 2026-08-31: no RedactedParagraphs, they are not necessary
- [x] P48 | Paragraph.original_column is deleted; Series.of_kind answers the question at all four sites, and the wrapped trailing block comment round-trips in four languages. | d894894 | Delete
      Paragraph.original_column and ask the SERIES instead, since every read of
      it was a truthiness test meaning is this paragraph beside code. Verify:
      the name appears in no signature or expression in src, Series.of_kind
      answers it, and the wrapped trailing block comment round-trips in C, Rust,
      Java and TypeScript
- [x] P49 | Paragraph.declares is deleted; cue_for moved to series.py and the lexer stamps the a cue directly, so attach never sees a docstring. The ordinal was the cue number all along. | 22af63a | Decide
      how a docstring finds its a place once Paragraph.declares is deleted,
      given that matching by anchor is ambiguous 11 times in 56 files
        > 2026-08-31 anchor alone collides 11x in 56 files -- measured 2026-08-31
- [x] P51 | Sheet.marks holds Mark, with unruled and refused beside it, off the wire. No container field is typed object; Mark.deserialize runs twice in a two-ruling collate run where it ran eight times; MalformedMark, _reconcilable and _keeps are deleted and the flow lost its DROP act with them. | b9ac7f8 | Update
      `desk.containers.Sheet` so `marks` holds `Mark` and the two kinds that are
      not a mark sit beside it -- `unruled`, the addresses handed over and not
      ruled on, and `refused`, an address with the reasons it did not parse.
      Works `containers-and-verification-are-unwired.md` T40. Verify: no
      container field is typed `object`; `problems_in`, `unruled`, `tally`,
      `verify_report` and `places` each call `Mark.deserialize` ZERO times, down
      from four parses of every ruled entry per run; and `_reconcilable`,
      `_keeps` and `MalformedMark` are gone, having had nothing left to do
        > 2026-09-01 Roy 2026-09-01: we clearly need sheet to take Marks not
        > 2026-09-01 Objects. Where the non-Marks go is Process 72
- [x] P52 | flows/mark_errors.py returns one Revisit per place a role must go back to -- role, address, reasons together, and whether the entry was unreadable -- in role then address order, copying no mark. Collated.revisit replaces Collated.unruled and _unruled_problems is gone from the command; CannotCollate carries the list so a refusal still reports it. | 0a0d856 | Implement
      `flows/mark_errors.py`, which reads `unruled` and `refused` across a
      stage's copies and returns every place a role must revisit as ADDRESSES
      AND REASONS -- never a rebuilt copy, `Process: #72`. Works
      `containers-and-verification-are-unwired.md` T41. Verify: the return names
      each address once with its reasons together and the role that owes it,
      copies no mark, and `commands/collate.py` assembles no findings of its own
      -- `_unruled_problems` moves into this flow
        > 2026-09-01 Roy 2026-09-01: mark_error_flow collects the errors and
        > 2026-09-01 makes something that helps the task agent point to the
        > 2026-09-01 correct ones for the role agents
        > 2026-09-01 THE BOUND IS NOT THIS STEP'S -- it goes in the task agent
        > 2026-09-01 briefing, and a-coverage-gap T2 holds it open
- [ ] P50 | Implement the command a reviewer runs to write ONE mark into its
      edit_copy, so a role fills a place without hand-writing JSON. Works
      `no-command-for-the-middle.md` T10. Verify: the command builds a `Mark`
      and writes `Mark.serialize()` into the addressed slot, refusing a mark the
      row's own instruction does not admit BEFORE it writes; the written
      edit_copy parses through `EditCopy.deserialize`; and `reviewer-brief.md`
      names it, which `Process: #41` emptied pending exactly this
        > 2026-09-01 Roy 2026-09-01: add the mark edit command to the todos that
        > 2026-09-01 need doing soon -- broken marks can still happen, but this
        > 2026-09-01 makes them less likely
- [x] P53 | text_at is public in desk/mark.py; a MOVE at its own address gives None and at claim.to gives change, both asserted by TestTextAtOneEndOfAMark | 586c13a | Move
      the Mark-to-text rule to desk/mark.py as public text_at. Verify: a MOVE at
      its own address gives None, at claim.to gives change
- [x] P54 | docket_of is in flows/revise.py; test_one_move_mark_yields_two_alterations asserts the origin delete and the destination text from ONE Mark | e8fe5d8 | Implement
      docket_of(copy) in flows/revise.py. Verify: one MOVE Mark yields TWO
      alterations, origin and destination
- [ ] P55 | Delete docket_from and _real_pages from desk/collator.py. Verify: no
      comment_review.docket import under src/../desk/
- [ ] P56 | Update _chief_copy's comment naming _real_pages. Verify: the symbol
      it names exists, or the comment is gone
- [ ] P57 | Update commands/proof.py to take --copy. Verify: --docket is gone
      and a chief edit_copy runs the revise
- [ ] P58 | Implement --to-docket, stopping after the transcribe. Verify: it
      writes a docket and nothing lands at --out
- [ ] P59 | Implement --from-docket, starting at the docket. Verify: it skips
      the transcribe and yields the same revise
- [ ] P60 | Update SKILL.md stage 7b for the new proof surface. Verify:
      test_skill_commands sees the invocation it names
- [ ] P61 | Implement the end-to-end chain test. Verify: census, distribute x4,
      collate, proof run with no Python by hand
