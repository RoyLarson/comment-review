# The mark and the collator -- the revise step, owned here

```
Status:   in-progress
Progress: 31 of 31 TODO tasks closed
Plan-tasks: 21 of 21 plan tasks closed
Owner:    backend
Requires-Roy: false
Raised:   2026-09-01 (two plans both held the revise step; Roy 2026-09-01 ruled it
          superseded in both and recreated here)
```

## Objective

The revise step: what a role may answer, what each answer comes to, and how a reversal is paired.

!! **THIS FILE REPLACED ONE OF THE SAME NAME ON 2026-09-01, AND THE OLD ONE IS
STILL HERE.** [`0.2.4-the-mark-and-the-collator-SUPERSEDED.md`](completed/0.2.4-the-mark-and-the-collator-SUPERSEDED.md)
holds all 67 of its boxes, 49 of them ticked -- **it is the record of what was
built, and its ids are live citations.** `docs/conventions.md` quotes its
`T6.1`, `T6.3` and `T4.3` by name, `docs/decision-log.md` cites more, and none
of those ids exists in this file. **Follow a `T<n>.<n>` to the superseded file;
follow a `P<n>` to this one.**

!! **AND IT WAS REPLACED BECAUSE THE TOOL COULD NOT READ IT.** The old file
carried its boxes under `## P1 --` ... `## P7 --` headings with no
`## Plan Tasks` section, so `job-board` reported it `0/0` and refused every
`add-t` against the whole directory -- *"checkbox lines but no ... section to
hold them"*. A plan whose counts are invisible is a plan nobody can be held to.

! **THE OTHER SIX WERE IN THE SAME STATE AND ARE NOT ANY MORE**, converted the
same day: two finished plans and one closed onto its TODO moved to
[`completed/`](completed/), and two were recreated through the tool like this
one. `EXCLUDED FROM THE BOARD` went `6 -> 0`.

!!! **WHAT THE MOVE ACTUALLY SETTLED IS OWNERSHIP, NOT FORMAT.** Making this
plan readable is what exposed the real defect: **two plans held the revise
step.** `0.2.4-the-commands-for-the-middle` claimed
[`a-revise-answer-has-no-artifact`](../../TODO/a-revise-answer-has-no-artifact.md)
`T4`, `T5` and `T7` and carried five steps for the diff-mark and the revise
round; this plan's `P6` carried eleven boxes for the same subject. Nothing could
see the duplication while one of the two was invisible to the tool.

! **ROY RULED IT HERE**, 2026-09-01: *"we supersede them in both locations and
recreate them here as owned here. And leave a note so we don't have questions on
what happened."* The branch is named for this plan, which is why the work lands
here rather than in the sibling.

| where | what it now says |
| --- | --- |
| `0.2.4-the-commands-for-the-middle`, section B | `T4`, `T5`, `T7` -- **MOVED to this plan** |
| the same plan's `P6`, `P8`, `P12`, `P18`, `P19` | **`[-]` SUPERSEDED**, naming this plan |
| the superseded file's `P5`/`P6` boxes | left as they were, ticked or not -- the record |

! **THE STEPS BELOW OPEN WITH AN ACT, AND THE OLD BOXES DID NOT.**
`docs/conventions.md` holds the old file up as its measured example -- *"38 of
67 boxes are prose, 25 name an action, and NONE poses a question"* -- so porting
them verbatim would have carried that defect across, and the tool's own grammar
check questioned three of them on the way in. **Each step names the box it came
from**, so the pair is readable from either side.

## !! THE MECHANISM HAD NO STEPS UNTIL 2026-09-02, ONLY ITS VOCABULARY

**`P1`-`P6`, `P13` and `P14` specify what a DiffMark may CARRY and which answers
are legal. Nothing planned the artifact, the send, the return, the recollate or
the terminator.** MEASURED on this file the day the gap was found: `DiffMark`,
`batch`, `send`, `recollat` and `terminat` returned **zero**; `diff-mark`
returned one, in prose describing a plan that had already been superseded.

! **AND THE HOLE IS EXPLICABLE.** The superseded plan carried *"five steps for
the diff-mark and the revise round"* -- `P6`, `P8`, `P12`, `P18` and `P19` of
`0.2.4-the-commands-for-the-middle`. On 2026-09-01 Roy ruled the revise step
belonged here, so the four TODO tasks moved across and **those five steps were
retired in place rather than re-created.** This plan was then written with the
answer-set boxes alone. `P16`-`P21` are the mechanism arriving.

!! **AND THREE OF THE FOUR TASKS IT WAS RECREATED FOR HAVE SINCE DISSOLVED.**
`T7` was agent variance and moved to the lane that owns it; `T5` was filed in
error; `T8` demanded a code-enforced cap that `Process: #78` rules is the task
agent's. **`T4` is the only live task this plan closes**, beside the `[?]` `T9`.

### What each of the six covers

`docs/the-turn.md` is the SOURCE for the loop; this is the map from that file
to these tasks. **It is not a sequence** -- a `P` is a task that makes a `T`
accomplishable, and the ordered list is an `SP`'s (`docs/conventions.md`).

| the part of the loop | task |
| --- | --- |
| a role's answer to a disagreement is its own artifact | `P20` |
| every disagreement goes out at once, one payload per role | `P21` |
| the answers come back and parse | `P16` |
| what resolved joins the chief's `edit_copy` -- **that is a round** | `P17` |
| the run says how many rounds it took, and of what kind | `P18` |
| at the cap, the chief rules on what is left | `P19` |

!! **`P3` NO LONGER SAYS THE RIGHT THING AND IS SUPERSEDED BY `P18`.** It reads
*"Update the round CAP to say what it counts"*, which presumes a cap the code
holds -- and `Process: #78` rules the cap is what the task agent is told, never
what the code enforces. **What survives is the counting**, which is `P18`.

## TODO tasks this plan closes

### A - [a-revise-answer-has-no-artifact](../../TODO/a-revise-answer-has-no-artifact.md)

- [-] T8 | T8 -- Enforce the two-round cap `Process: #9` already ruled. Verify:
      a third round cannot start, the place reaches the copy chief carrying
      every round's marks, and the run reports the rounds each place took.
- [-] T4 | T4 -- Route a `query` by shape. Verify: `human-review-necessary`
      never returns to a role, and `unable-to-determine` carries the question
      into the next ask.
- [-] T5 | T5 -- Count queries first raised at revise. Verify: the run reports
      the number, and it is zero on a set of marks where every query was raised
      in round one.
- [-] T7 | T7 -- A reversal is a row, paired with whoever LAST set the
      statement. Verify: stage 3 reversing a paragraph stage 2 set pairs with
      stage 2 and not with stage 1, read from the revise's per-place provenance.
- [x] T10 | Implement the DiffMark a role answers a disagreement on, four
      answers closed. Verify: a fifth, or any of Mark's seven, is refused by
      name
- [x] T11 | Implement the batch: one payload per role per round, each slot
      carrying its diff3. Verify: one send per role whatever the place count
- [x] T12 | Implement the return: a role's answered batch parses at the
      boundary. Verify: an unanswered slot is refused by name, never read as
      withdraw
- [x] T13 | Implement the recollate so a round's resolutions join the chief's
      copy. Verify: a lone surviving add, all others holding, lands
- [x] T14 | Decide whether a composition re-read is answered with a DiffMark or
      a fresh Mark, given clean and query are its only passes
- [x] T15 | Implement the conflict outcomes: hold/hold next round, hold/withdraw
      takes the held in, withdraw/withdraw re-asks. Verify: each lands
- [-] T16 | Implement the round counter, rounds named by kind, no maximum
      enforced. Verify: a run reports each place's rounds
- [x] T17 | Implement the copy chief's ruling at the cap on whatever is still
      unresolved. Verify: no place survives the last round unruled
- [x] T18 | Implement routing of a refused or unanswered DiffMark to its role as
      a revisit. Verify: it appears in revisit naming role and address
- [x] T19 | Generate the DiffMark contract a role is handed from the code, as
      allowed() does for Mark. Verify: no agent file hand-types its fields
- [x] T20 | Implement the turn counter, turns named by kind, no maximum
      enforced. Verify: a run reports each place's turns
- [x] T21 | Update batch_of so a reread seeds a Mark slot over the composed
      text, an escalation a DiffMark. Verify: no DiffMark field on a reread
- [x] T22 | Implement Determined, the chief's per-place record: stet, taken_in,
      recast. Verify: Mark's seven and DiffMark's four are refused
- [x] T23 | Update MasterProof to carry the turn record and one Determined per
      resolved place. Verify: a serialized proof round-trips both
- [x] T24 | Update _chief_copy so the chief's edit copy is derived from the
      Determineds. Verify: every place in it names its Determined
- [-] T25 | Update run_turn so a Determined keeps the turn the place first
      agreed on. Verify: agreed on turn 1, it reads turn 1 after turn 2's fold
- [x] T26 | Decide what a query at a place other roles are contesting records,
      since it takes no part in the fold. Verify: it is on the master proof
- [x] T27 | Update run_turn to pair an answer with the sent slot by address, not
      by an echoed question key. Verify: a slot returned without it parses
- [x] T28 | Decide whether byte-identical change texts agree when the
      instructions or quoted sentences differ. Verify: the ruling is on the log
- [x] T29 | Decide whether a lone owing mark against cleans is a stet at turn 0
      or goes back to the other roles as a re-read. Verify: ruling on the log
- [x] T30 | Decide whether a place agrees when every owing mark is
      byte-identical or when a majority is, the rest holding. Verify: ruling on
      the log
- [x] T31 | Update Determined.deserialize to refuse a null mark unless side is
      ORIGINAL, and an unknown side. Verify: both refused by name
- [x] T32 | Update the fold so byte-identical change texts agree whatever the
      instruction or sentence quoted. Verify: patch + correct, one text -> stet
- [x] T33 | Update the fold so a lone owing mark is a re-read to every role that
      marked the place but a query. Verify: one mark, three cleans -> re-read
- [x] T34 | Implement the human-review query riding on the master proof,
      unsettlable and asked last; other shapes abstain. Verify: it is on the
      proof
- [x] T35 | Update the recollate so a place once stet leaves every later batch
      and keeps its turn. Verify: stet at turn 1 reads turn 1 after turn 2

### B - [no-command-for-the-middle](../../TODO/no-command-for-the-middle.md)

- [x] T13 | Implement the check a role runs over its copy or batch before
      returning it, on the fold's own boundaries. Verify: refusals named

## Plan Tasks

- [x] P1 | FINISHED -- every slot names its question; parse_answers refuses the other set by name (a Mark on an escalation, a DiffMark on a composition) | 2c04181 | Update
      the revise row so it SAYS which question it asks, and refuses a role that
      answers one question with the other's answer set. Was T6.7
- [x] P2 | FINISHED -- COMPOSITION_ANSWERS derived from the rows; contracts() publishes it; the brief's copy is the agents lane's T14 | 28981c8d | Implement
      the COMPOSITION answer set, derived and not hand-typed, published in the
      brief. Was T6.8
        > 2026-09-04 Process 86: the set is Mark's clean, query, correct, patch
- [-] P3 | SUPERSEDED by P18 -- it presumes a cap the code holds, and Process #78 rules the cap is what the task agent is told; the counting survives as P18 | d261916 | Update
      the round cap to say WHAT it counts, reporting each place's rounds by KIND
      rather than as one number. Was T6.11
- [x] P4 | FINISHED -- DiffInstruction: hold, withdraw, correct, patch, closed; a fifth or Mark's seven refused by name | 5574f0a | Implement
      the CONFLICT answer set, ruling `hold` and `withdraw` in or out by name
      rather than by silence. Was T6.12
- [x] P5 | FINISHED -- hold/hold, hold/withdraw, withdraw/withdraw each reach a named outcome; an escalation answers with DiffMarks only | d4d7cd8 | Implement
      the outcomes for a conflict, so no composition outcome is reachable from a
      conflict row. Was T6.13
- [x] P6 | FINISHED -- COMPOSITION_ANSWERS (clean, query, correct, patch) against DiffInstruction; hold and withdraw a conflict's alone, correct and patch legal in both | bc62ea5 | Implement
      the rule saying which answers are exclusive to each question and which are
      legal in both. Was T6.14
        > 2026-09-04 Process 86: hold and withdraw are a conflict's alone
- [x] P7 | FINISHED -- ruff, ruff format, ty, the floor, check_vocabulary and claude plugin validate green; build --check red by the repo's own rule until the release builds plugins/ | b1ccce3a | Update
      the release gate command list so it names the checks this repo now runs,
      then run them all green. Was G1
- [x] P8 | FINISHED -- check_vocabulary: 11 retired words, 0 uses in the shipped tree, run on the merged branch | b1ccce3a | Delete
      any shipped use of the retired field noun, or show there is none. Was G2
- [x] P9 | FINISHED -- one mutation per new check, 14 of 14 caught after the one miss became a test; the table is in tests/README.md | ddcb435f | Implement
      test coverage for each new module, proven by a mutation of each new check
      being caught. Was G3
- [x] P10 | FINISHED -- test_determined, test_turn, test_diff_mark and test_check_command each name their source in the header | 28981c8d | Update
      each new test file to name where its expectations came from. Was G4
- [x] P11 | FINISHED -- Process 82-91 on the log; the collate polysemy (copy-desk sense kept, bibliography's collation declared) at vocabulary.md:98-106 | 7e2c6b4 | Update
      docs/decision-log.md with every ruling this branch produced, and declare
      the `collate` polysemy in docs/vocabulary.md. Was G5
- [-] P12 | SUPERSEDED -- the fallback left the tree with prototype/ on 2026-08-25; the live boundary refuses by name (Process 92); nothing running to delete | 70450a91 | Delete
      the unreachable claim fallback, once its ruling lands. Was G7
- [x] P13 | FINISHED -- reconcile's entries carry marks, no rendered text; batch_for is the renderer at the flow | 28981c8d | Update
      reconciliation to return escalations carrying no rendered text, and
      implement the renderer. Was T5.3
- [x] P14 | FINISHED -- clean adopts or withdraws, query rides or abstains, correct and patch fold again; test_turn.py | 99c7620 | Implement
      the outcomes for a composition re-read, so every answer reaches a named
      one. Was T6.10
- [x] P15 | FINISHED -- the remainders are filed: no-command-for-the-middle T14-T16, stage-5 T13; 345f3f41 names them | 345f3f41 | Implement
      the filing of anything this plan does not finish, before it closes. Was G6
- [x] P16 | FINISHED as a prototype -- parse_answers pairs to the sent slot, refuses by name, returns Revisits (T27, T18) | f4e746b | Implement
      the return, so a role's DiffMarks parse at the boundary. Verify: an
      unanswered place is refused, never read as a withdraw
        > 2026-09-03 PROTOTYPE, not settled -- desk/diff_mark.py
- [x] P17 | FINISHED as a prototype -- run_turn applies, folds again, keeps earlier stets; a lone add lands (T13, T33, T35) | aefefdd | Implement
      the recollate, so resolved places join the chief's edit_copy. Verify: an
      unresolved place stays out and is named
        > 2026-09-03 a lone add re-reads forever until this lands -- measured hand 3
        > 2026-09-04 Process 87: a recollate writes one Determined per resolved place
- [x] P18 | FINISHED as a prototype -- Determined.turn on every place, MasterProof.turns, a stet keeps its turn (T20, T35) | aefefdd | Implement
      the round counter, naming each round by kind. Verify: a run reports the
      rounds it took and enforces no maximum
        > 2026-09-04 the word is turn -- Vocabulary 33; T16 superseded, re-filed
- [x] P19 | FINISHED as a prototype -- rule_at_cap taken_in or recast; determined_chief refuses an unruled place (T17) | d1ddbd8 | Implement
      the copy chief's own ruling at the cap. Verify: no place survives the last
      round unruled
        > 2026-09-04 Process 87: cap = taken_in or recast; the original author is a side
- [x] P20 | FINISHED as a prototype -- desk/diff_mark.py, the four closed, Mark's seven refused by name (T10) | 5574f0a | Implement
      the DiffMark container, carrying hold, withdraw, correct or patch. Verify:
      a Mark is refused where a DiffMark is owed
        > 2026-09-03 PROTOTYPE, not settled -- desk/diff_mark.py, 5574f0a
- [x] P21 | FINISHED as a prototype -- batch_of, one send per role, seeded by question (T21) | bc62ea5 | Implement
      the batch that gathers every disagreement into one payload per role.
      Verify: one send per role whatever the place count
        > 2026-09-03 PROTOTYPE, not settled -- desk/diff_mark.py, 9406b3b
        > 2026-09-03 Roy: the diff rides with the DiffMark; the FLOW attaches diff3
        > 2026-09-04 Process 86: a reread seeds a Mark slot, not a DiffMark -- T21
