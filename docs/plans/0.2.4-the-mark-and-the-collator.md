# 0.2.4 -- the mark, the revise, and the collator

**The middle.** A role hands back marks; something checks each one against the page it rules on,
reconciles what several roles said about one place, emits a conflict for the ones that disagree,
and **pulls a revise so the next stage reads what the last one settled.** That is `collator.py`
and the revise pull, and neither exists.

**Branch:** `feat/the-mark-and-the-collator`, off `feat/0.2.4-what-a-reviewer-is-handed`.

!! **STILL 0.2.4, AND THE NUMBER IS A CLAIM RATHER THAN A CHAPTER.** Roy, 2026-08-28: *"We don't
have an operational backend and until we do we can't ship this."* ! The version does not advance
because a branch feels like new work; it advances when what it names can be shipped. Every plan
under 0.2.4 is the same release still being made.

! **The rulings this plan implements are `decision-log.md Vocabulary: #17` to `#23`, `Process: #9`,
`#10` and `#33`.** Nothing here re-opens them.

## The shape this plan is written against

!! **THE ROLES DO NOT ALL READ THE SAME PAGE AT THE SAME TIME, AND NEVER DID.** Roy, 2026-08-28,
on the first draft of this plan: *"it bakes in the idea that all editorial-role agents see
everything at the same time and only rule on it once."* `SKILL.md` stage 4 already runs
`ownership-context` alone at 4a and the other three at 4c, and at least one further agent is
expected before those three.

```
                 ORIGINAL  (--repo .)
                      |
   enriching stage    |   facts -> into the next binder. No docket, no revise
                      v
   stage 1  ownership-context          sheet -> marks
                      |                 verify -> reconcile -> revise step -> docket
                      v                 proof_setter --out revise-1  +  prove_unchanged
                 REVISE 1  (a tree copy, the drafts overlaid)
                      |                 ASSERT the address set equals the original's
                      v
   stage 2  block / function / module, one message, blind to each other
                      |                 same four steps
                      v
                 REVISE 2   ==   the draft the human approves at 7a
```

Every editorial boundary is the same four steps -- **verify, reconcile, revise, pull** -- and the
last one's output is what 7a reads. There is no separate backward phase and no stage is ever
re-entered.

| | holds because |
| --- | --- |
| **the address space never moves** | `prove_unchanged` holds the code byte-identical, and a place is determined by CODE. Gated by re-censusing each revise |
| **the forward pass is a line** | a reversal is a ROW on the revise sheet at that boundary, between the two roles that disagree -- not a re-run |
| **one root per stage** | a `source` citing a page an earlier stage edited must read that page's current text |
| **one mechanism, four uses** | the pull feeds the next stage, feeds the revise step, feeds 7a, and runs `prove_unchanged` every time |
| **the run terminates** | two rounds per place, then the copy chief -- `Process: #9`, ruled 2026-08-24 |

! **THE MECHANISM IS MOSTLY BUILT.** `flows/proof_setter.py` already sets a page and writes a
draft into `--out DIR` -- *"a temporary file, never the original"* -- proving executable code
unchanged and refusing a draft directory that overlaps the repo. And `census`, `carry`, `proof`,
`prove_unchanged` and `referrers` all already take `--repo`, so pointing a stage at a revise is
passing a different value.

## What is IN, and what is deliberately not

| | |
| --- | --- |
| **in** | the mark's shape and its gate; the revise and `taken_in`; source-verification; reconciliation; the `diff3` conflict; the diff-mark and the reversal |
| **not** | the **copy chief**. Roy, 2026-08-28: *"I think not yet - the reconciliation step will pass stuff to it next."* Reconciliation and the revise step EMIT escalations and resolve nothing |
| **not** | the reference write chain -- [`a-reference-needs-its-own-write-chain`](../../TODO/a-reference-needs-its-own-write-chain.md), deferred until the middle works |
| **not** | **`code_concerns` on the sheet** -- Roy, 2026-08-28: *"T1.6 is indefinitely deferred."* It was `T1.6` of P1 until then. The work lives on in [`code-concerns-cannot-carry-a-proposed-change`](../../TODO/code-concerns-cannot-carry-a-proposed-change.md), nine tasks whose Status already reads `blocked` |

!! **AND `T1.6` LEFT THE TASK LIST RATHER THAN SITTING IN IT UNCHECKED, WHICH IS THE WHOLE POINT.**
`CLAUDE.md` rules that **an unchecked box says the work is still to do** and that the release gate is
**every box on the plan ticked** -- so an INDEFINITELY deferred task left in P1 does not describe a
pause, it makes 0.2.4 unreleasable forever, by arithmetic nobody chose.

! **A PLAN AND A TODO HAVE DIFFERENT LIFETIMES, and that is why this move is the correct one rather
than a tidy-up.** A plan is one release's scope and it CLOSES; a TODO's lifetime is indefinite and
progress is made as we see fit. *"Indefinitely deferred"* is a statement about lifetime, so the item
belongs in the tracker built for it. `conventions.md` already says so: anything in a plan that does
not get done is filed in `TODO/` before the plan closes -- and here it was already filed, so nothing
had to be written down to preserve it.

!! **AND IT WAS FILED ON THE WRONG TODO, WHICH IS A SECOND FINDING.** Roy, 2026-08-28: *"It should
not have been added to this todo."* `the-ported-mark-does-not-fit-the-brief` T4 said *"give the sheet
a `code_concerns` list"* -- but that file's Objective is that **a mark written from the brief
VERBATIM is refused by the gate**, the gate and the instruction disagreeing about what a valid mark
is. A new container on the SHEET is not that subject, and it was **already tracked**: nine tasks on
[`code-concerns-cannot-carry-a-proposed-change`](../../TODO/code-concerns-cannot-carry-a-proposed-change.md),
T1 defining the shape and T3 publishing it in the brief. **Misfiled AND duplicated.**

! **THAT T4 IS NOW CHECKED AS SUPERSEDED, NOT AS DONE** -- `CLAUDE.md`'s table gives superseded work
an `[x]` so the record of it stays legible, and *"a deleted box leaves no trace that it was ever
there, or why it went."* The work itself remains OPEN on the file that owns it.

! **NOTHING IS LOST AND NOTHING IS FINISHED.** **Deferred is not done**, and moving where a thing is
tracked does not do it.

! **WHAT IT WAITS ON, named rather than left open-ended:** a file that states what the SHEET carries.
`decision-log.md Process: #37` records what it cost to have no such file for the MARK -- eleven
fields entered `src/` unapproved. Adding a key to the sheet before its shape is stated would repeat
that exactly. ! The double-bind behind it -- that the review promises no code changes while the code
concerns are real -- is on
[`a-role-with-no-code-out-damages-the-prose`](../../TODO/a-role-with-no-code-out-damages-the-prose.md).

!! **THE PROSE THAT SHIPS IS UPDATED IN THIS BRANCH, NOT AFTER IT.** Roy, 2026-08-28: *"Add the
updated commands/words to the brief, agent files, and the instructions generator. We don't want to
accidentally leave behind stuff this important."*

! **AND THE LANE LINE IS HIS**: *"How the agents function and the specific rules that are used to
encourage the agents to do better is the agents lane. Giving them the information necessary to run
the Python part is fine."* So a field name, a command, a key list, a generated table, and the fact
that a role is holding revise 2 rather than the original are this branch's. **How much deference an
earlier stage's edit is owed is not.**

## The constraint every step is written under

!! **NO TEST IN THIS BRANCH TAKES ITS EXPECTATION FROM THE CODE UNDER TEST.** Roy, 2026-08-28:
*"Tests that test themselves are not useful tests."* `decision-log.md Vocabulary: #23`.

| | |
| --- | --- |
| an **input** may come from reality | a real page through `page_for`, a real binder, the 706 recorded marks |
| an **expectation** may come from | the shipped prose that states the contract, a recorded run's output, or a literal a human checked |
| an expectation may **never** come from | the module being tested |

! **MEASURED, on the suite written the night before this plan**: 47 tests passed over a gate that
refused two marks written from the brief verbatim, because every case was built from the same
table the gate reads. **Mutation-checking did not catch it** -- breaking the code broke the test,
since both sides moved together. A check can bite and still ask the wrong question.

## What "done" means

A stranger can run one command per stage over a filled sheet and get a revise plus a diff for
every conflict, with every number in this plan re-derivable from the round-2 marks in
[`evidence/the-loop-measured-2026-08-27/`](../../evidence/the-loop-measured-2026-08-27/).

---

## P1 -- the mark fits the brief, in both directions

!! **THE DEFECT THIS EXISTS TO CLOSE IS ALREADY LIVE, IN CODE WRITTEN 2026-08-27.** MEASURED
2026-08-28: a mark written from the shipped brief VERBATIM is refused by `desk/mark.py` in two
places -- an `add` carrying `claim.anchor`, and a `query` carrying `claim.shape`.

! **ONE ROOT CAUSE, AND THE AUTHORITY IS THE BRIEF.** `reviewer-brief.md` publishes the keys each
instruction's `claim` owes. `claim_keys` stated only `claim_all` and derived none of the rest, so
`allowed()` published `query`'s three SHAPE VALUES as though they were claim KEYS. Four traits
name keys the brief lists: `claim_any` -> `shape`, `needs_attempted` -> `attempted`,
`needs_settles` -> `settles`, `needs_anchor` -> `anchor`.

!! **THIS SENTENCE CITED `prototype/` UNTIL 2026-08-28, AND THAT WAS THE WRONG AUTHORITY.** Roy:
*"Why are you talking about code in `prototype/original/`? And fixing things based upon something
in there?"* ! `prototype/` is a record of how it once worked -- *"Nothing imports it, nothing ships
it, it does not run."* **What the gate OWES comes from the shipped prose a role reads**, and how a
dead module happened to do it is archaeology. ! The tests were already right: every expectation
came from the brief or a checked literal. It was the REASONING that leaned on the record, which is
worse in a plan than in a test, because a plan is what the next person reads.

! **AND THE TESTS PASSED**, because `a_mark()` built its claim from the same wrong assumption. The
suite was mutation-checked, which proves a rule can FAIL and cannot prove it is the RIGHT rule.
**Only an independent source found it**, which is why every task below verifies against the brief
rather than against the table.

- [x] **T1.1** Restore the extras derivation in `claim_keys`, so one row states every key a
      `claim` owes. *Works [`the-ported-mark-does-not-fit-the-brief`](../../TODO/the-ported-mark-does-not-fit-the-brief.md) T1.* Verify: `allowed()['claim']['query']` names `shape`,
      `attempted`, `settles`, and `add` names `missing`, `anchor` -- matching the brief exactly.
- [x] **T1.2** Make `problems()` read those keys. Verify: the two brief-compliant marks above are
      ACCEPTED, and a `query` naming a shape that is not one of the three is refused.
      *Works [`the-ported-mark-does-not-fit-the-brief`](../../TODO/the-ported-mark-does-not-fit-the-brief.md) T2.*
      ! It lands the GATE half of [`record-and-verdicts-disagree`](../../TODO/record-and-verdicts-disagree.md)
      T4 and does not tick it -- that box's own verify names ROUTING, which is T4.3's.
- [x] **T1.3** *Works that file's T3.* Rebuild `tests/test_mark.py` so no case is built from `INSTRUCTIONS`. Cases come
      from the brief's published table and from the 706 recorded marks. Verify: the new suite
      FAILS against today's `mark.py` before T1.1 and passes after -- a suite that passes both
      ways is testing itself.
- [x] **T1.4** Rename the field to `instruction` in `desk/mark.py`, `flows/marks.py`, the brief,
      the four agent files and `vocabulary.toml`. Verify: `grep -rn "verdict"` over `plugins/`
      returns nothing outside a quoted ruling. *`Vocabulary: #17`; a one-for-one substitution.*
- [x] **T1.5** Put `raw_text` -- **the paragraph, not the page** -- on every seeded row, taken
      from the revise the binder names. Roy: *"I don't want to have to figure out indentation
      again or comment style. All of the agents can read the page again on their own."* Verify: a
      seeded sheet round-trips to that root's exact bytes for the paragraph, indentation and
      comment markers included.
> **`T1.6` WAS HERE AND IS OUT OF SCOPE.** Roy, 2026-08-28: *"T1.6 is indefinitely deferred."* It
> read *"give the sheet a `code_concerns` list."* ! **The number is retired, not reused** -- T1.7
> onward keep their own, so a report, a commit or a ledger line naming `T1.6` still resolves. See
> *What is IN, and what is deliberately not*, above, for where the work lives and why an
> indefinitely deferred box could not stay in a plan.
- [x] **T1.7** *Works that file's T5.* Rebuild the generator. `reviewer-brief.md:278` carries
      `<!-- BEGIN GENERATED ... prototype/render_brief.py -->` and **that script exists nowhere in
      the tree**, so the table announces it is generated while being hand-maintained. Verify: the
      generator writes the block from `INSTRUCTIONS`, and a test fails if the committed block and
      a fresh render disagree.
- [x] **T1.8** State `ran` in the brief -- the command that settled a claim.
      *Works [`the-fields-do-not-say-a-mark-may-cite-across`](../../TODO/the-fields-do-not-say-a-mark-may-cite-across.md)
      T3.* Verify: the brief names it and says a claim settled by execution without it is
      incomplete.
- [x] **T1.9** State in the brief that a source may cite any place in the LIBRARY, with the
      disagree-and-cite rule and its corollary. *Works that file's T1, T2 and T5.* Verify:
      `grep -rn` for the rule returns exactly one path, and no agent file restates it.

!! **T4 OF THAT FILE IS NOT THIS LANE'S, AND THE PLAN SAID IT WAS.** It reads *"Say what a role
does when it cannot tell which side of a disagreement is wrong"* -- **behaviour**, not a field's
definition. Roy's line, 2026-08-28: *"Stating the commands and what they do is acceptable for this
role. How they get used and what order and the process that gets the agents to use them is
agents."* ! The file is `agents`-owned and `Requires-Roy: true`; **T1.8 and T1.9 state what a FIELD
is and what it may hold**, which is the program's own shape and is why they are here.

! **T2 OF THAT FILE CARRIES A SUPERSEDED FACT** -- it says `change` is a LINE ARRAY. Raw text, per
[`docs/the-mark.md`](../the-mark.md). Its other two stand. A dated note on the file records it; the
task is worked with the correction rather than reworded.
- [x] **T1.10** DROP the four dead command invocations from `SKILL.md` -- `vocabulary
      --reviewer` (592), `run_context --template`/`--check` (617, 619), `verdicts` (741).
      *Works [`the-skill-names-commands-that-moved-to-prototype`](../../TODO/the-skill-names-commands-that-moved-to-prototype.md)
      T4.* Verify: `check_vocabulary.py` exits 0, and every `comment-review.py <name>` left in
      `SKILL.md` names something `__main__.py` lists in `COMMANDS`.

!! **DROPPED, NOT MARKED DEAD.** Roy, 2026-08-28: *"Drop the commands from the brief and from
the task agent/managing-editor ... We will fill the commands section back in later."* ! The
alternative considered was CLAUDE.md's own treatment of these same commands -- leave them under a
banner saying they moved. **Instructing an agent to run a command that does not exist is worse
than saying nothing**, and a marked-dead command still spends the budget `docs/limitations.md`
holds this file to.

! **THE CONSEQUENCE IS STATED RATHER THAN HIDDEN**: stage 4 then has no step that emits a role's
vocabulary, and stage 5 no citation gate. **Both are already absent from the code** -- the prose
was describing a system that stopped existing on 2026-08-25. Refilling them is that TODO's T1 and
T2, and is not this branch's.

! **`galley` AT ~918 IS NOT DROPPED, because it fails differently.** The NAME resolves through a
real alias to `proof`, so it reaches live code; it spells `--census/--edits` where `proof` takes
`--binder/--docket`, so the parser refuses it. **T1.11 is what fixes it**, under the ruling below.

! **AND `record` AT 643 AND 666 IS A FIFTH, found by the task that dropped the four.** It wrote
the gate, watched it fail on `record`, and deleted the gate rather than commit it red or invent an
exemption it had no ruling for. That was correct then. **T1.12 is the gate, made complete.**

- [ ] **T1.11** Document EVERY live command accurately in every agent-facing file -- `SKILL.md`,
      the four `agents/*.md`, `reviewer-brief.md` and the other `references/*.md`. Name what each
      command IS, what it does, its flags and its output. This includes fixing `galley`'s flags at
      ~918 and resolving `record` at 643 and 666. *Works
      [`the-skill-names-commands-that-moved-to-prototype`](../../TODO/the-skill-names-commands-that-moved-to-prototype.md)
      T2 and T3.* Verify: every command named in an agent-facing file appears in `COMMANDS`, and
      every flag written beside it is accepted by that command's own parser.
- [ ] **T1.12** Gate it. Verify: a test parses every `comment-review.py <name> <flags>` out of the
      agent-facing files and checks the name against `COMMANDS` and each long flag against that
      command's `argparse` -- and it FAILS when a flag is changed to one the parser does not take,
      which is the case `galley` proves is not hypothetical.

### !! A CLOSED SET IS A `StrEnum`, AND THE CLI PUBLISHES IT

Roy, 2026-08-28, on the agent file that told a role to write `"outside my role"` where the gate
takes `outside-my-role`: *"Should be a StrEnum with an appropriate flag on the cli to make it
work"*, and *"Same for all of the other flags and input definers."*

!! **THE CONVENTION ALREADY EXISTS IN THIS REPO AND `desk/` NEVER GOT IT.**
`reading/series.py` holds `Kind(StrEnum)`, and its docstring records the exact failure this
repeats: *"Which strings paired up was known only to a hand-kept tuple in a third file -- so when
`trailing-comment` left one of those tuples on 2026-08-20, nothing structural noticed, and the two
tuples drifted."* ! The sets that skipped it are the ones that arrived in the prototype port.

! **AND THE FLOOR TRAP IS ALREADY MEASURED**, `lexer.py:87`, 2026-08-22: `x in SomeEnum` **raises
`TypeError` on 3.11** and only answers False from 3.12. Re-confirmed 2026-08-28 on 3.11.15. **The
established idiom is a companion frozenset** -- `LAYOUT = frozenset(Layout)` -- and membership is
asked of THAT, never of the class.

**MEASURED 2026-08-28: 37 argparse arguments across `commands/`, and ONE carries `choices=`.**

| closed set | where | today |
| --- | --- | --- |
| the three `query` shapes | `desk/mark.py:53` | a tuple of three module constants |
| the seven instructions | `desk/mark.py` | `INSTRUCTIONS`' keys, bare strings |
| the chain's steps | `flows/proof_setter.py:62` | `STEPS`, a tuple |
| the command names | `__main__.py:23` | `COMMANDS`, a tuple |

- [ ] **T1.15** Each closed set above becomes a `StrEnum`, following `series.Kind`, with a
      companion `frozenset` for membership and its **value DERIVED from its name**. Verify: no
      site asks membership of an enum class -- `grep` finds none -- no member's value is
      hand-typed, and the suite passes unchanged, since a `StrEnum` member IS its string.

!! **THE THREE FORMS ARE RELATED AND NOT IDENTICAL, AND THE RELATION IS MECHANICAL.** Roy,
2026-08-28: *"You know that the difference between `QueryEnum.OUTSIDE_MY_ROLE` and its `__str__`
and the flag `--outside-my-role` all have to be related but not exact."*

| form | | how it is got |
| --- | --- | --- |
| identifier | `OUTSIDE_MY_ROLE` | **the source of truth** |
| value / `str()` | `outside-my-role` | `name.lower().replace("_", "-")` |
| flag | `--outside-my-role` | `"--" + member` |

```python
class QueryShape(StrEnum):
    @staticmethod
    def _generate_next_value_(name, start, count, last):
        return name.lower().replace("_", "-")

    OUTSIDE_MY_ROLE = auto()
```

! **`_generate_next_value_`, NOT `__new__`.** Roy: *"while I don't usually like monkeying with
`__new__` you can always slide that in the middle to make `QueryEnum("outside-my-role")` work."*
**MEASURED 2026-08-28 on 3.11.15: that lookup already works with no help at all** -- enum lookup by
VALUE is standard and the value IS the kebab string. What needs the intervention is the other
direction: **deriving the value so it can never be hand-typed out of step with the name**, which is
the drift that produced this whole item.

!! **AND `_missing_` HAS NO SEAT ANYWHERE. THE QUESTION IT ANSWERS DOES NOT ARISE.** Roy,
2026-08-28: *"If it is a cli flag there is no missing it is a true and it gets set. I don't think
checking if it is close earns anything other than telling us that we can program correctly."*

| where a closed-set value could be read | what handles a wrong one |
| --- | --- |
| a **flag** -- `--outside-my-role` | argparse. It is boolean: matched, or *unrecognized arguments* |
| an **option taking a value** -- `--role X` | argparse `choices=`, which refuses and prints the set |
| `claim.shape`, out of a filled JSON sheet | the gate, against the companion frozenset, with its own message |

! **NO SITE CALLS THE CONSTRUCTOR ON UNTRUSTED INPUT**, so there is nothing for `_missing_` to
intercept. ! A session had argued for `_missing_` as a near-miss ERROR rather than a near-miss
acceptance; that argument was aimed at a boundary that does not exist, and a *"did you mean"* is
self-congratulation rather than a check.
- [ ] **T1.16** Every CLI surface for a closed set is one of the two forms above -- a boolean
      **flag** named from the member, or an **option carrying `choices=`** drawn from the enum.
      Verify: `--help` prints the valid values or flags for each, a wrong one is refused by the
      parser with the set named, and **no command hand-writes a list of valid values**. ! **That
      is what makes the CLI the instruction set for spellings** -- Roy: *"What finishes can be put
      into the instruction set and the cli help."* A role reads the spelling off the tool instead
      of off prose that can drift.

### !! THE MARK'S SHAPE HAD NO OWNING FILE, AND SOMETHING ELSE BECAME THE SPEC

Roy, 2026-08-28: *"None of those were part of the accepted shape of the mark structure or any part
of the plan. This is not okay. We had a specific plan for the mark structure and the appropriate
classifiers for each part."* And: *"That is the shape that I approved going into this plan. I had
not knowledge of the other shape."*

!! **THE APPROVED SHAPE IS NOW [`docs/the-mark.md`](../the-mark.md)** -- seven fields, FOUR
classifier columns, SEVEN row flags, and two pieces of prose that decide nothing. It was promoted
from the captured snapshot in `evidence/the-loop-measured-2026-08-27/`, which stays as it is and
now points forward.

! **HOW THE OTHER SHAPE ARRIVED.** A session was asked whether the mark type was usable and whether
it needed a CLI endpoint. It began by naming `prototype/original/record.py`'s `--seed`/`--check`
verbs and its checkers; **Roy interrupted mid-sentence** and ruled *"copy it from there and update
the rules/requirements from there ... It belongs in the desk/."* **The only thing on screen was the
ENDPOINT.** The port copied the `Instruction` dataclass whole, and a 22-field scheme entered `src/`
having never been proposed. ! Every review this branch commissioned asked whether the gate matched
the BRIEF's key table -- it does -- and none asked whether the classifier scheme was approved.

**MEASURED 2026-08-28: 22 fields, and ELEVEN have no home in the approved shape.**

    change_all, change_help          `move`'s "showing BOTH" is ONE change-column fact
    claim_any                        the three query shapes are a module constant, not a row field
    needs_attempted, needs_settles   `query`'s claim KEYS -- they belong in the claim-keys list
    owes_address, owes_claim, owes_reason   defaults, and only `clean` deviates
    removes                          no column and no flag
    payload, claim_help              PROSE. A row carries none

!! **`change` IS THE UPDATED PARAGRAPH AS RAW TEXT, AND THAT IS WHAT `change_all`/`change_help`
WERE PATCHING.** Roy, 2026-08-28: *"`change` needs to be the updated paragraph as raw text not
lines or sentences ... The original had it as one sentence to change but that did not work which is
why you probably put in prose because it is a hole with that part of the spec without it."*

! **MEASURED, and it confirms the diagnosis exactly**: `move`'s row carries `change_all=("to",)`
and a `change_help` reading *"move needs the DESTINATION paragraph in `change`, as `to` -- plus
`from`, the origin as it reads after"*, and **those two fields exist for no other reason.** An
underspecified column grows a prose field to explain itself.

! **THE SEEDED `raw_text` AND THE RETURNED `change` DIFF DIRECTLY**, which is what T1.5 and P5 both
need: base = `raw_text`, sides = each role's `change`. ! It SUPERSEDES the four rounds' array-of-
lines form; raw text makes the two measured transcription failures LOUDER, not quieter.

!! **AND THE PROSE IS NOT TO BE GENERATED EITHER.** Roy, 2026-08-28: *"What finishes can be put
into the instruction set and the cli help. **I forbid you from including anything like this in the
code right now.**"* ! A session had proposed deleting the two fields and DERIVING their sentences
from the claim-keys list -- which puts prose-building in the code to avoid storing prose in the
code. **Neither the string nor the generator belongs there**; the explanation lives in
[`docs/the-mark.md`](../the-mark.md), in the brief that publishes it, and in the CLI help.

- [x] **T1.13** Rebuild `Instruction` to the approved shape -- four columns and seven flags, and
      nothing else -- deleting the eleven above. **Add no replacement machinery for the prose.**
      Verify: `allowed()['claim']` is unchanged for all seven, and `tests/test_mark.py` and
      `tests/test_mark_brief.py` pass **with no edit**, since their expectations come from the
      brief and not from the module.
- [x] **T1.14** Gate the classifier set against the spec. Verify: a test reads the classifier and
      flag names out of `docs/the-mark.md` and asserts `Instruction`'s fields are EXACTLY those --
      failing both when a field is added to the dataclass without the file changing first, and
      when a field carries prose. ! **That is the check whose absence let this happen**, and its
      expectation comes from prose the code cannot move.

!! **THE LANE LINE, REFINED.** Roy, 2026-08-28: *"Stating the commands and what they do is
acceptable for this role. How they get used and what order and the process that gets the agents to
use them is agents. **Having accurate and complete command instructions are on you because it is
part of the program.**"*

| | |
| --- | --- |
| **`backend`** | what a command IS, what it does, its flags, its output. Accurate and COMPLETE |
| **`agents`** | how they get used, in what order, and the process that gets a role to run them |

! **SO THE COMMANDS SECTION IS NOT BLOCKED ON ANOTHER LANE**, and treating it as though it were
was the error T1.10 was written under. What waits on `agents` is the STAGE CHOREOGRAPHY; the
command reference is this lane's and is owed now. ! Roy, on the same day: *"We will fill the
commands section back in later"* -- T1.11 is that refill, and it is scheduled rather than deferred.

## P2 -- the revise, and telling a role it is holding one

The spine. Everything after this step means *per stage*. `Process: #10` gives the artifact its
name -- *"the second proof, pulled after the marked corrections have been set"* -- and this plan
extends it from the second to the Nth.

- [x] **T2.1** A stage list as DATA, each entry naming its kind. *Works
      [`the-flow-assumes-every-role-reads-at-once`](../../TODO/the-flow-assumes-every-role-reads-at-once.md)
      T1.* Verify: an `editorial` entry pulls a revise and an `enriching` one does not, and adding
      a stage is a row rather than a code change.
- [x] **T2.2** The binder records which revise it was censused from. *Works that file's T2.*
      Verify: a sheet seeded from it names the revise and the original in its header, and a binder
      built from the original says so rather than leaving the field absent.
- [x] **T2.3** Pull a revise at each editorial boundary -- the settled docket through
      `proof_setter` into a tree copy with the drafts overlaid. *Works that file's T3.* Verify:
      the revise root holds every library file, only the scheduled pages differ, and
      `prove_unchanged` passes on it.
- [x] **T2.4** Gate the address space, which is the whole safety argument. *Works that file's T4.*
      Verify: re-censusing a revise yields the address set the original yielded, over real files
      -- and the check FAILS on a revise whose code was changed by hand.
- [x] **T2.5** Every read for a stage resolves against that stage's root. *Works that file's T5.*
      Verify: a `source` citing a page an earlier stage edited returns the REVISE's text, not the
      repo's.
- [x] **T2.6** `taken_in` -- original against the revise in a role's hand, as a unified diff per
      page, plus which stage took in which addresses. *Works that file's T6.* Verify: it prints
      nothing when no stage has set anything, and its diff applies cleanly to the original.
- [x] **T2.7** The last revise IS the 7a draft. *Works that file's T7.* Verify: no second
      draft-building path exists, and the artifact 7a reads is the final revise root.

## P3 -- source-verification

Per MARK, against the page it rules on, in the root that stage was given. `Vocabulary: #19`.

!! **NO SHA CHECK, AND NO RE-READING THE PAGE.** Roy, 2026-08-28: *"Too early for the strictness
and the look up time each time."* ! Nothing ruled says a mark must be refused because its page
moved, and asking costs a read per mark. **What the copy chief sees through the proof-setter pass
is where a stale edit surfaces** -- Roy, on the same morning: *"copy-chief gets to see an update
and compare through the normal proof-setter pass before it moves on and it can find the broken
edits somehow."*

! **AND THE TWO BEST CHECKS ARE FREE, because of T1.5.** With the paragraph on the sheet, *does
the address resolve* and *is the quoted sentence really in it* need no file read at all. Only a
`source` cite into another file costs one, and those are read through a per-file cache -- the 706
recorded marks carry 382 citations over 9 roots, so the reads collapse.

- [x] **T3.1** The address resolves to a place the binder carries. Verify: a mark whose address
      names no place is refused, and one naming a place the binder dropped says so by name.
- [x] **T3.2** Every `source` resolves and its `verbatim` is really within reach of the cited
      line. *Works `record-and-verdicts-disagree` T3 -- "stop `load_report` dropping a bare-string
      source before `source_problem` runs".* Verify: a bare-string source is refused, not dropped.
- [x] **T3.3** The sentence the claim rules on -- `false`, `drop`, `from` -- is really in that
      paragraph. Verify: a paraphrase is refused; the check names which key failed.
- [x] **T3.4** Prove each check can fire on a real page. *Works that file's T8 -- "replace
      `address_problem` with a check that CAN fire".* Verify: a mutation of each check is caught
      by a test.

!! **THIS LINE CLAIMED T5 AND T6 TOO, AND IT WAS WRONG.** The P3 implementer declined to tick
them and said why: both name defects in `prototype/original/record.py:453` and
`prototype/original/desk.py:170-172`, which it *"never read or ran"*, and **avoiding the same
defect CLASS is not verifying those tasks' own criteria.** ! That is the correct call, and the
plan was the thing at fault -- I wrote the citation without checking where those tasks point.

## P4 -- reconciliation

Per PLACE, across the marks OF ONE STAGE. Emits; decides nothing.

! **CROSS-STAGE IS SEQUENCE, NOT RECONCILIATION.** A later stage reads what an earlier one
settled as ordinary text on the page; there is nothing to merge. Where it *reverses* that text,
the row goes to the revise step -- P6 -- and not here.

- [ ] **T4.1** Group every mark by every place it TOUCHES -- its address, and a `move`'s
      destination. *Works [`collate-buckets-a-move-at-one-end`](../../TODO/collate-buckets-a-move-at-one-end.md)
      T1 and T2.* Verify: T1's failing case -- a `move` from `a0` to `a8` against another role's
      `correct` on `a8` -- escalates, and fails on today's single-key bucketing.
- [ ] **T4.6** A `move` settles or escalates WHOLE. Verify: a `move` whose destination escalates
      does not have its origin settled, and no docket ever carries one end of one.

!! **BECAUSE A `move` IS INDIVISIBLE.** Roy, 2026-08-28: *"It really is two operations wrapped in
one label and justification. Which is right -- you don't want to say it can move and it can't
complete the move because 1/2 is rejected."* ! **Half a move is a defect neither half reports**:
prose deleted and never landed, or landed and never removed. `docs/the-mark.md` states the rule and
what it binds at every stage.
- [ ] **T4.2** The settle/escalate table, keyed on the SENTENCE and never on the proposed text.
      Verify: two roles of one stage on different sentences of a paragraph MERGE; two on the same
      sentence escalate.
- [ ] **T4.3** A scope-declaring `query` is a boundary report and does not block the other roles.
      *Works [`record-and-verdicts-disagree`](../../TODO/record-and-verdicts-disagree.md) T4's
      ROUTING half -- the gate half landed at `fcba2a6`.* Verify: three `clean` and one
      `outside-my-role` settles rather than escalating -- the case measured dropping a docket from
      12 alterations to 5; and a `query` carrying no `shape` reaches the chief rather than being
      read as a scope declaration. *`Process: #33`.*
- [ ] **T4.4** `unable-to-determine` is settled where another role of the same stage ruled
      substantively on the same place. *Works `the-fields-do-not-say-a-mark-may-cite-across` T7.*
      Verify: role A undetermined + role B `correct` does NOT reach the chief.
- [ ] **T4.5** Re-derive the measurement. Verify: the round-2 marks -- three concurrent roles,
      which is exactly one stage under P2 -- settle **13 of 16** places and escalate 3, from
      `evidence/the-loop-measured-2026-08-27/`. *Works `collate-buckets-a-move-at-one-end` T3.*

## P5 -- showing a difference, in git's spelling

One module, two renderings, ruling on nothing. `Vocabulary: #20`.

- [ ] **T5.1** Render base plus both edits, `diff3` style. *Works [`a-conflict-has-no-rendering`](../../TODO/a-conflict-has-no-rendering.md) T1, T2 and T4.* Verify: two edits on
      DIFFERENT lines of one paragraph render as disjoint, and the same pair in the two-sided
      form does not -- the measurement that chose the format.
- [x] **T5.2** The unified page diff `taken_in` prints, from `difflib` and no git process -- the
      revise root is a temp copy and need not be a repo. Verify: the rendered diff applies to the
      original and reproduces the revise byte for byte.
- [ ] **T5.3** The rendering is separate from the detecting. *Works that file's T3.* Verify:
      reconciliation returns escalations carrying no rendered text, and the renderer takes an
      escalation and returns lines.

## P6 -- the revise step: the diff-mark, and the reversal

Its own artifact, its own closed set. `Vocabulary: #22`.

!! **A REVERSAL IS A ROW HERE, NOT A RE-RUN.** Roy, 2026-08-28: *"The return to stage 1 only goes
between the agents that disagree over the statement. It doesn't restart the whole flow. Same as
the other revise and in the same revise step."*

- [ ] **T6.1** `hold`, `withdraw`, `correct`, `patch` -- and nothing else. *Works [`a-revise-answer-has-no-artifact`](../../TODO/a-revise-answer-has-no-artifact.md) T1.* Verify: the set is
      derived from `rules_on_text` plus the two answers, so it cannot drift from `INSTRUCTIONS`.
- [ ] **T6.2** A revise sheet addressed to a ROLE, carrying the rendered diff and only the rows
      that role is party to. *Works that file's T2 and T6.* Verify: a role party to one place in a
      stage it did not otherwise join receives a one-row sheet, and an unanswered row is neither
      held nor withdrawn.
- [ ] **T6.3** The four outcomes. *Works that file's T3.* Verify: any new mark relitigates; two holds escalate; hold plus
      withdraw takes the held claim in; two withdraws pick one and re-ask.
- [ ] **T6.4** A reversal row, paired with whoever LAST set the statement. *Works that file's T7.*
      Verify: stage 3 reversing a paragraph stage 2 set pairs with stage 2 and not with stage 1,
      read from the revise's per-place provenance.
- [ ] **T6.5** Enforce the two-round cap `Process: #9` already ruled. *Works that file's T8.*
      Verify: a third round cannot start, the place reaches the copy chief carrying every round's
      marks, and the run reports the rounds each place took.
- [ ] **T6.6** A `query` at revise routes to the HUMAN and is counted. *Works that file's T4 and T5.* Verify:
      it never returns to a role, and the run reports how many were first raised at revise.
      *`Vocabulary: #21`.*

---

## Release gate

Every box above ticked, and:

- [ ] **G1** `uv run ruff check .`, `uv run ty check src/comment_review/`, the py3.11 floor gate,
      `check_vocabulary.py`, `build_plugin.py --check` and `claude plugin validate` all green.
- [ ] **G2** No shipped file says `verdict` for the field, and none says the retired noun.
- [ ] **G3** `tests/` covers each new module, and a mutation of each new check is caught.
- [ ] **G4** No test takes its expectation from the module it tests. Verify: for each new test
      file, name where its expectations came from -- the brief, a recorded run, or a checked
      literal.
- [ ] **G5** The four rulings this branch produced are in `docs/decision-log.md`, and the
      `collate` polysemy is declared in `docs/vocabulary.md`.
- [ ] **G6** Anything in this plan not done is filed in `TODO/` before the plan closes.
- [ ] **G7** [`claim-fallback-is-unreachable`](../../TODO/claim-fallback-is-unreachable.md) is
      finished -- its **T4** ruled and its **T8** landed. Verify: that file is closed, and no
      shipped comment describes a fallback the code does not take.

!! **G7 IS ROY'S, 2026-08-28: *"Needs to be finished before we can ship 0.2.4."*** It was
`Status: deferred` at 6 of 8 and named in NO plan, so **the release gate could not see it.** A
TODO nothing cites is invisible to the thing that decides readiness, which is the failure
`conventions.md` describes from the other direction -- *"a plan that names no TODO cannot be
closed from either end."*

! **ITS TWO OPEN BOXES SPLIT ACROSS US.** `T4` is a `*` ruling -- whether a non-object `claim`
preserves the reviewer's words or is emptied; Roy leans (ii), recorded 2026-08-22 and not yet
ruled. `T8` is backend's, and **cannot start until T4 answers**, because it makes the prose agree
with whichever shape the ruling picks.

! **AND THE CONDITION IT WAS DEFERRED ON IS THE ONE THIS BRANCH RESOLVES.** Its `T7` records the
deferral as waiting on *"the determination of the census to findings to verdicts system"* -- Roy,
2026-08-22: *"that whole system ... is something that needs to be determined now that the backend
part of the system works."* **That determination is what P1 through P6 are.** The wait is ending,
which is why the ruling is now worth asking for.
