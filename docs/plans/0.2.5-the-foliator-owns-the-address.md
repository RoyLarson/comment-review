# 0.2.5 -- the foliator owns the address

**Branch `fix/folio-placement-is-not-where-the-anchor-is`. The release is the FOLIATOR: one walk
that emits every address and the anchor it is attached to, owned by one module, so nothing else
computes a place.** 0.2.4 settled what an address IS. This settles who MAKES one.

! **This is a `docs/plans/` plan, not a `docs/superpowers/plans/` one.** A superpowers plan is
written for an engineer with no context -- exact files, TDD steps, a commit per task. This is a
release scope: what ships, what does not, and what must be true first.

! **A PLAN is not a TODO.** Todos remain open indefinitely and progress as we see fit; a plan is a
scope of work to be completed in one run. Anything here that does not get done is filed in
`TODO/` before this plan closes, or it is lost.

## The gate

!! **THE RELEASE GATE IS EVERY BOX BELOW TICKED.** Not "the tests pass": 672 passed continuously
while the `b` foliator was uninitialised, `b0` and `b1` were mutually exclusive, and the test
written to prevent exactly that asserted an f-string's punctuation instead of the property.

! **Every box names the TODO it works**, the TODO names the work, and the work is in the tree --
so a stranger who did none of this can check any box. A box only its author can verify is a
judgement wearing a checkbox.

! **A `*` box is a ruling only Roy can make.** Asking for one is work; asking whether the work is
finished is a missing artifact.

! No progress count here -- `scripts/todo_tool.py` manages `TODO/` and not this file. **The boxes
are the state.**

## Phase 1 -- the foliator is a thing, not an expression

- [x] **F1 -- three foliators, as closures or classes, each holding its own foliation AND its
      anchors.** DONE `7e32e74`. `foliator.Foliator` holds a counter and `places`, folio -> the
      line of code it is attached to; `emit` takes the number and records the anchor in one step,
      `skip` steps past a trigger a series does not emit for. 13 tests in `tests/test_foliator.py`
      drive it off `tests/fixtures/python_edge_cases.md`. Previously `a` read
      `paragraph.get("declares")`, `b` was `sum(1 for n in code if n < at) + 1` and `c` was
      `code.index(start) + 1`. Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

- [ ] **F2 -- the walk is initialised at `<module>` and rolls forward.** HALF DONE `7e32e74`: the
      walk emits `b0` at the module trigger unconditionally and `b1` above the first line of code,
      and `test_b0_AND_b1_BOTH_EXIST` holds it. ! **The remaining half is that no PARAGRAPH
      occupies `b1` yet** -- `intervals()` still decides which gaps get one by its own rule, so on
      a file with front matter the place exists and nothing sits in it.
      `tests/test_edge_cases.py` carries the `expectedFailure` that pins this and will report an
      unexpected success when it lands. Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

- [x] **F3 -- no folio is computed from a line number.** DONE 2026-08-20. `address()` (87 lines),
      `gap_step()` (23) and `on_step()` (12) are DELETED, and `grep -c "def address\|def
      gap_step\|def on_step" foliator.py` answers 0. Every folio now comes from `Foliator.emit`
      and is read back by `Foliation.above`/`.beside`/`.documents`. About 30 test sites drove the
      old path and were repointed at the page's walk.

      ! **THIS BOX WAS TICKED FALSELY ONCE, on 2026-08-19, and un-ticked on 2026-08-20.** The
      claim then was the same sentence, and all three functions were still in the file with the
      arithmetic intact. What was true was that nothing SHIPPED called them -- which is exactly
      how a claim passes a green suite. It is kept here rather than tidied away, because this
      file's gate section says a box must be re-derivable by someone who did none of the work, and
      the failure was that nobody re-derived it, including its author. Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

- [x] **F4 -- the emitted table is `address -> anchor`, recorded by the walk.** DONE `b342cd1`.
      `census.anchor_every_address()` is deleted -- 89 lines, with its `beside` map and its
      `FRONT_MATTER -> MODULE` special case. ! It also closed the split that let the two disagree:
      anchoring ran inside `census_for` and addressing in the run loop afterwards, so every test
      that called `census_for` directly received a census with anchors and no addresses and
      nothing said so. Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

- [ ] * **F5 -- RULING: the closing trigger.** Seven lines of code need nine `b` places -- `b0`
      at the module, `b1..b7` above each line, and one for the gap AFTER the last. Is that the
      module again, an explicit EOF trigger, or does `b` emit N+1 per walk? Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

- [ ] **F6 -- the test holds the PROPERTY, not the punctuation.**
      `test_a_folio_is_never_DERIVED_from_another` forbids two f-string forms and both
      expressions survive verbatim with `+ 1` appended. Replace with: every folio came from a walk
      that emitted it. Works [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md).

## Phase 2 -- one module makes an address

- [x] **A1 -- the foliation is the ONLY place an address is made**, in both directions. DONE
      `79f1a92`, `b342cd1`. `foliate()` assigns the foliation; `Foliation.above/.beside/.documents`
      answer *which address does this line belong to right now*, and `attach()` ties a paragraph
      to a place the walk already emitted. `census.py` computes no address. Works
      [`census-owns-addressing`](../../TODO/census-owns-addressing.md).

- [x] **A2 -- the census stops setting anchors.** DONE `b342cd1`. The anchor now comes from
      `foliation.places`, the walk that emitted the address. ! `lines_of_code` copies each line's
      characters from that line's own `c` and never re-cuts the line -- 20 tests caught the
      version that did, because `'    return os  # why'` is not the anchor its `c` answers with.
      Works [`census-owns-addressing`](../../TODO/census-owns-addressing.md).

- [ ] **A3 -- each of the four modules announces ONE subject**: `foliation` names places, `page`
      says what a page and a paragraph are, `census` says which prose occupies which address,
      `galley` sets the proposed text. Verify by running `module-context` over all four. Works
      [`census-owns-addressing`](../../TODO/census-owns-addressing.md).

- [ ] **A4 -- the galley applies edits BY SERIES, never by line.** `b0`, then `a0`, then
      a -> b -> c. `splice` sorts on `(start, end, column, replacement)` and applies descending by
      line, which is what makes an `a`/`b` tie fall to the reviewer's quote style. Works
      [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md) and closes
      [`galley-is-still-index-keyed`](../../TODO/galley-is-still-index-keyed.md).

      !! **THE ORDER IS UNIVERSAL, AND IT IS AN APPLICATION ORDER -- NOT A LINE ORDER.** Roy,
      2026-08-20: *"`a` has to be put in first because it is the 'inner most' documentation -- the
      `b` gets applied in the rows outside-beyond it."* Two edits inserting at ONE point are not
      a conflict: the second lands ABOVE the first, so applying `a` first puts `b` outside it. A
      Python docstring sits INSIDE the body and a Rust `///` sits ABOVE the `fn`, and the same
      order is right for both -- no language needs to state which side its doc goes on. `b0`
      before `a0` is the same rule, applied to the module.

      !! **MEASURED 2026-08-20, and neither half is expressible today.** `splice` re-sorts its
      own argument, so `[a, b]` and `[b, a]` produce BYTE-IDENTICAL output -- the caller cannot
      say the order at all. The tie therefore falls to the ASCII of the replacement text: `//` <
      `///` puts Rust's gap prose outside the doc comment, which satisfies the ruling **by
      accident**, while `"""` < `#` applies Python's comment first and leaves the docstring ABOVE
      it, which does not.

## Phase 3 -- a page is a page

!! **THE SPLIT, ruled 2026-08-20.** Roy: *"is it the census's job or should it have been split up
by the galley or the page? Seems like the census's job should be to take the output of all of the
pages and reformat it into the (most) usable format for the agents."* The vocabulary already said
so -- **the census is the enumeration across EVERY page in scope, not one page as a list** -- so
building one file's paragraphs was never its work:

| | owns |
| --- | --- |
| `foliation` | names places: the walk out, the lookup back |
| `page` | ONE FILE -- its paragraphs tied to the places on it. It builds itself |
| `census` | every page in scope, formatted for the agents |
| `galley` | the proposed text, set as files |

- [x] **P1 -- `page.py` defines a `Page`, and builds one.** DONE 2026-08-20. `page_for` returns
      a `Page` carrying the path, the text, the paragraphs, the FOLIATION and the tier -- it
      returned a bare list and dropped four of those, so a consumer needing one re-derived it from
      the file or made the caller carry it. It iterates, indexes and sizes as its paragraphs,
      because a page IS its paragraphs in order. `.prose` is what a reviewer owes a record on.
      ! An UNPARSED file carries an EMPTY foliation rather than an invented one: the walk never
      ran, because the code lines were never established. 7 tests.

      ! The move landed in three commits -- the builder and the generators off the census, then
      `lexer.py` for the language readers, then the type. `census.py` 1,759 -> 531, `page.py`
      566, `lexer.py` 1,011. Works [`census-emits-no-page`](../../TODO/census-emits-no-page.md)
      and [`census-owns-addressing`](../../TODO/census-owns-addressing.md).

- [ ] **P2 -- the census STACKS PAGES for review, and a page is the whole page.** Roy: *"the
      pages should actually be the pages, not just a small piece of the pages."* What a reviewer
      gets today is printed rows under a `== path` heading, and `--filtered` collapses runs, so it
      is not even a complete list. Works
      [`census-emits-no-page`](../../TODO/census-emits-no-page.md).

- [ ] **P5 -- the page BEATS BOTH PRIOR FORMATS, or it does not ship.** Roy, 2026-08-20: the
      page is *"partially to get the best of both worlds."* That is not a preference between
      formats, it is a claim that DOMINATES both, and it fails if either half fails:

      | | information | tokens | recall |
      | --- | --- | --- | --- |
      | unfiltered, v0.1.0 | all | high | **worse** |
      | filtered, v0.2.0 | less | low | baseline |
      | the page | MORE than filtered | LESS than unfiltered | AT LEAST filtered |

      !! **THE PRIOR IS ALREADY MEASURED AND IS WRITTEN DOWN NOWHERE.** Roy, 2026-08-20, on the
      v0.1.0 -> v0.2.0 split: going unfiltered, *"the agents got a lot more tokens and used a lot
      more tokens on effectively the same level of output. They did miss a lot in the
      difference."* ! Nothing in `evidence/` records it and the shipped tree carries only the byte
      figures -- 39% of the listing was repeated paths, `--filtered` saved 61%. **The measurement
      that justifies the single most consequential thing about what a reviewer sees exists in run
      history and nowhere a reader can find it.** Recovering it is the first half of this box.

      ! `evals/grade_hazards.py` scores a run from its DIFF against twelve planted hazards, which
      is the oracle for the second half. Blocked on
      [`the-harness-cannot-run-the-system-it-grades`](../../TODO/the-harness-cannot-run-the-system-it-grades.md),
      because a hand-run comparison is the thing that TODO exists to end.

- [ ] * **P3 -- RULING: what a page RENDERS as.** The file with its addresses in the margin is
      the obvious candidate and is what makes an anchor readable without a second lookup. Works
      [`census-emits-no-page`](../../TODO/census-emits-no-page.md).

- [ ] **P4 -- verified on the edge case.** A role handed the PAGE can see that `a0` and `b1` sit
      either side of the module docstring, which no row list shows.
      `tests/fixtures/python_edge_cases.md` is the fixture and
      `tests/test_edge_cases.py` the test; its `expectedFailure` on `b0` flips to an unexpected
      success when F2 lands. Works [`census-emits-no-page`](../../TODO/census-emits-no-page.md).

## Phase 4 -- review, only after the above

!! **NOT BEFORE.** Each of these reads a diff; running them over a tree mid-restructure grades
work that is about to be replaced. Ordered as Roy specified, all from `3af9752`.

- [ ] **R1 -- `/simplify` from `3af9752`.**
- [ ] **R2 -- `/code-review medium` from `3af9752`.**
- [ ] **R3 -- `/code-review medium` from `3af9752`.**
- [ ] **R4 -- `/code-review medium` from `3af9752`.**
- [ ] **R5 -- `/simplify` from `3af9752`.**
- [ ] **R6 -- every finding from R1-R5 is filed in `TODO/`** before this plan closes. A finding
      recorded only in a review transcript was never recorded.

- [ ] **R7 -- NO `expectedFailure` SURVIVES THIS PLAN.** Every one passes, or it is removed with
      the reason recorded. Roy, 2026-08-20: *"they need to pass or be removed when done with this
      plan, and that gate should be added to the end of the plan."*

      !! **AN xfail IS A DEFERRAL WITH NOBODY'S NAME ON IT.** It was the right call while the
      implementation moves underneath -- *"it doesn't help to keep them alive when the
      implementation is going to slip out from under them"* -- and it is the wrong thing to ship:
      a red test announces itself and a green one is checked, while an xfail is neither. It also
      cannot be read off the box count, which is what makes it a gate rather than a habit.

      ```bash
      # Zero, or name each survivor and why. ! The DECORATOR, not the word --
      # `test_edge_cases.py` discusses a past one in prose and is not a hit.
      grep -rn "@unittest.expectedFailure" tests/
      ```

      **Standing at 2026-08-20: 8**, all in `test_galley.TestAnIntervalIsInsertedInto` -- the
      galley's interval contract, deferred when an empty place stopped reporting a range. Works
      [`galley-is-still-index-keyed`](../../TODO/galley-is-still-index-keyed.md).

! **Three `/code-review medium` runs, not one.** The same reason this repo dispatches four
reviewers rather than one: a single pass is one sample of a stochastic reader.

## Phase 5 -- the round trip

- [ ] **T1 -- draw up the ROUND-TRIP plan**: a file to a census, a list of verdicts back to a new
      file, and it must do the edge case. Roy: *"it has to be able to do my edge case at least."*
      ! This box is satisfied by the plan EXISTING in `docs/plans/`, not by the round trip
      working -- that is the next release.

## Not in scope

- **The `block` -> `paragraph` rename in identifiers** -- ~1,935 sites,
  [`a-block-is-a-paragraph-on-a-page`](../../TODO/a-block-is-a-paragraph-on-a-page.md). The
  shipped prose already says paragraph; the identifiers are their own scope.
- **The front-matter restamp** --
  [`front-matter-restamps`](../../TODO/front-matter-restamps.md). F2 removes the `b0`/`b1`
  collapse that made it visible, but deciding front matter from the PROSE rather than from
  position is a separate change, and the general question -- annotations are not stable under this
  tool's own edits -- is larger than this release.
- **Migrating held artifacts in `evidence/`.** Every `b` folio shifts when F2 lands; whether they
  are migrated or pinned to the old scheme is a box on
  [`b-foliator-uninitialised`](../../TODO/b-foliator-uninitialised.md), not this plan.
- **A version bump.** Nothing under `plugins/` changes behaviour until F1-F4 land; the number
  moves when the release is cut, in one commit across all three files.
