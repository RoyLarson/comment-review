# The binder trim -- what is left after six of eleven landed

!! **THIS FILE REPLACED ONE OF THE SAME NAME ON 2026-09-01**, and the old one is
[`0.2.4-rework-the-binder-hands-the-repo-SUPERSEDED.md`](0.2.4-rework-the-binder-hands-the-repo-SUPERSEDED.md).
Follow a `P<n>` there for the original eleven; each step below names the box it
came from.

!!! **IT READ `0 of 11` AND WAS `6 of 11`.** Audited 2026-09-01 by checking each
box against the TODO tasks it names, and those against the tree:

| box | how it was verified |
| --- | --- |
| `P1`, `P2`, `P6` | every TODO task they work is closed |
| `P3` | MEASURED: a row emits `anchor`, `cue`, `original_start`, `original_end`, `raw_text` -- one spelling of absent, where three were named |
| `P4` | MEASURED: `grep -rn '"tier"' src/` returns nothing |
| `P5` | MEASURED: the page envelope carries `path` and `sha`; **no row carries a path** |

! **AND THE TODO BOARD WAS STALE THE SAME WAY.**
[`census-row-carries-empty-fields`](../../TODO/census-row-carries-empty-fields.md)
**T12** -- *"Move `path` to a page envelope, stated once"* -- reads open and is
done by that same measurement.

!! **THE TWO RULINGS MAY BE MOOT, WHICH IS WHY THEY ARE `[?]` AND NOT TICKED.**
`P1` and `P2` below ask whether a reviewer needs a separate view, and what a
reviewer is actually handed. **The design appears to have answered both in
practice**: a role is handed a seeded `edit_copy` by `distribute --seed`, and
`reviewer-brief.md` tells it to FILL that record. ! **That is an observation
about what the code does, not a ruling** -- these were Roy's `*` boxes, and
whether practice settles them is his to say.

! **`P3`-`P5` ARE LISTING DEFECTS, AND THE LISTING IS NO LONGER WHAT A REVIEWER
IS HANDED.** `listing-hands-the-repo`'s own `Demoted:` note measured it:
*"census.py --json returns 115 rows and 73,792 bytes with or without
--filtered, byte-identical, and holds no NOT CHECKED block at all. So a reviewer
handed the JSON pays nothing for these 279 lines."* They are real defects of a
human-facing output, not of the reviewer path.

```
Status:   in-progress
Progress: 0 of 6 TODO tasks closed
Plan-tasks: 0 of 5 plan tasks closed
Owner:    backend
Requires-Roy: true
Raised:   2026-09-01 (audited 2026-09-01: the plan read 0 of 11 and was 6 of 11)
```

## Objective

The listing defects and the two rulings left over from the binder trim, after P1-P6 landed and their boxes were never ticked.

## TODO tasks this plan closes

### A - [listing-hands-the-repo](../../TODO/listing-hands-the-repo.md)

- [ ] T2 | T2 -- State `NOT CHECKED` once per run rather than once per page.
      Verify: a second page's listing does not repeat it.
- [ ] T3 | T3 -- Make its paths repo-relative. Verify: no listing holds an
      absolute path.
- [ ] T4 | T4 -- Re-measure the listing after both. Verify: before and after,
      from named commands, are written into this file.

### B - [census-row-carries-empty-fields](../../TODO/census-row-carries-empty-fields.md)

- [ ] T7 | T7 -- Measure the trim in bytes and in the filtered listing, before
      and after. Verify: both numbers from named `--json` and `--filtered` runs
      are written here.

### C - [the-census-is-mostly-intervals-nobody-rules-on](../../TODO/the-census-is-mostly-intervals-nobody-rules-on.md)

- [ ] T6 | T6 -- Test that a filtered census carries every prose paragraph's
      ADDRESS unchanged from the full one. Verify: a multi-file census test
      fails if any address differs.
- [ ] T19 | T19 -- * Rule the page rendering, deferred by Roy 2026-08-21; the
      open questions are in the Objective. Verify: the ruling is in
      `docs/decision-log.md`.

## Plan Tasks

- [?] P1 | Decide whether a separate reviewer view is still needed, now that a
      role is handed a seeded edit_copy. Was P7
- [?] P2 | Decide what a reviewer is handed, which the design appears to have
      settled as the seeded edit_copy. Was P8
- [ ] P3 | Update the NOT CHECKED report so it is stated once per run, not once
      per page. Was P9
- [ ] P4 | Update the listing so its paths are repo-relative. Was P10
- [ ] P5 | Implement the before-and-after trim measurement, written into both
      TODOs. Was P11
