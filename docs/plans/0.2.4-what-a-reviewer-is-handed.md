# What a reviewer is handed -- stage 4 serialises, and the release gate

!! **THIS FILE REPLACED ONE OF THE SAME NAME ON 2026-09-01**, and the old one is
[`0.2.4-what-a-reviewer-is-handed-SUPERSEDED.md`](0.2.4-what-a-reviewer-is-handed-SUPERSEDED.md).
Follow a box there for the original twenty-one; each step below names what it
came from.

!! **IT READ `16 of 21`, AND ONE OF THE FIVE OPEN BOXES WAS ALREADY DONE.**
*"Every script whose output the run keeps takes `--out`. Today exactly one
does"* -- MEASURED 2026-09-01, **five do**: `census`, `collate`, `distribute`,
`proof` and `referrers`. The others report to stdout and keep nothing; `carry`
writes back through `--binder`. That box is closed on the record.

!!! **AND ONE OF THE FOUR THAT REMAIN IS THE WHOLE OF THE STAGE-4 WORK.** `P1`
below works **T4-T14** of
[`ownership-is-read-first-but-nothing-makes-it-so`](../../TODO/ownership-is-read-first-but-nothing-makes-it-so.md)
-- eleven tasks, **every one of them open**. It is not a gate item sitting
beside three others; it is the plan's remaining subject.

! **THE OTHER THREE ARE THE RELEASE GATE** this plan was never taken through:
the version in three files, `claude plugin validate`, and checking its own TODO
claims against the board rather than against itself.

```
Status:   in-progress
Progress: 0 of 11 TODO tasks closed
Plan-tasks: 0 of 4 plan tasks closed
Owner:    backend
Requires-Roy: false
Raised:   2026-09-01 (audited 2026-09-01: it read 16 of 21 and one of the five open
          boxes was already done)
```

## Objective

Stage 4 runs ownership-context alone and the other three against its resolved placement; plus the release gate this plan was never taken through.

## TODO tasks this plan closes

### A - [ownership-is-read-first-but-nothing-makes-it-so](../../TODO/ownership-is-read-first-but-nothing-makes-it-so.md)

- [ ] T4 | T4 -- **Create the module that carries a PROPOSAL on a node.**
      Verify: the module exists, and `census.py` gains no proposal field.
- [ ] T5 | T5 -- **Write down the fields a PROPOSED tag carries**: verdict,
      resolved owner, proposing role. Verify: the list is in exactly one file.
- [ ] T6 | T6 -- **Make T4's module implement every field T5 names.** Verify: a
      4a report round-trips into the module and back with each named field
      preserved.
- [ ] T7 | T7 -- **Gate 4b's INDICES: census B may differ from census A only
      where a 4a record names.** Verify: the gate refuses a census B changed at
      an unnamed index.
- [ ] T8 | T8 -- **Gate 4b's FIELDS: a changed field must be one the verdict
      licences.** Verify: the gate refuses a census B changed in an unlicensed
      field.
- [ ] T9 | T9 -- **Say in `SKILL.md` which fields 4b may change.** Verify: `##
      Stage 4` names 4b and lists them.
- [ ] T10 | T10 -- **Say in `SKILL.md` that *"Nothing is on disk yet"* holds at
      4b.** Verify: `## Stage 4` states it, and adds no numbered rule elsewhere.
- [ ] T11 | T11 -- **Give `run_context.py`'s packet a PASS line naming 4a or
      4c.** Verify: `--check` refuses a 4c packet that names census A.
- [ ] T12 | T12 -- **Fix the packet's success message at `run_context.py:348`**,
      which still instructs the parallel dispatch. Verify: the success message
      names 4a and 4c.
- [ ] T13 | T13 -- **Teach the collator not to read a PROPOSED-drop node as
      ordinary prose.** Verify: a census carrying a PROPOSED drop owes no
      verdict on that node.
- [ ] T14 | T14 -- **Teach the collator what an `add`'s filled interval is.**
      Verify: a census whose interval gained proposed text joins without a
      manufactured coverage gap.

## Plan Tasks

- [ ] P1 | Implement the stage-4 serialisation, so ownership-context runs alone
      and the other three run against its resolved placement
- [ ] P2 | Update the version in pyproject.toml, CHANGELOG.md and plugin.json in
      one commit
- [ ] P3 | Run claude plugin validate over the shipped plugin, which no test
      replaces
- [ ] P4 | Update this plan so every TODO task it claims is checked closed
      against the job board
